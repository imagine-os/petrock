# 0014 - Grooming day / board / agenda / form / detail, customers, pets, vaccine queue, inbox, notifications, invoice

version: 0.2.0 (proposed, merged by the integrator)
date: 2026-09-18
prompt: 0005 (module assignment "frontdesk-grooming-people F-30..F-59"; log in docs/prompts/_pending/frontdesk-grooming-people.md)
intent: Give the front desk the grooming day at a glance and everything about people and pets: a Figma-faithful grooming day view with per-user groomer columns, a fresh kanban board (D-008), the agenda list, a groom booking form priced from the tables, an appointment detail like the Figma booking detail, customers and pets with the Customer / Pet Details forms, a vaccine verification queue whose "Verify" approves the pet and confirms pending bookings, a staff inbox with assignment, staff notifications and a printable invoice.
decision: pending decisions in docs/decisions/_pending/frontdesk-grooming-people.md (D-118..D-129); rules R-X60..R-X68 owned by this module.
rejected: a second status vocabulary for appointments (kept the core APPOINTMENT_STATUS enum and mirrored the booking transitions + PIN gates instead); hand-rolled tables / buttons in pages (everything goes through the library, 14 new components with metas); hardcoded prices (the form and detail call quoteGrooming with packages / addons / fees / taxes); editing shared files (no change to App, registry, schema/index, tokens, CLAUDE.md, kanban, decisions.md; supabase/schema.sql and docs/data-model.md / docs/specs.md are regenerated outputs).
files: src/modules/frontdesk-grooming-people/** (index, specs, lib, hooks, module.css, 15 pages), src/components/molecule/{GroomingDateNav,GroomAppointmentCard,GroomStatusBadge,PetVaccineChip,DeskLookupSelect,DeskAttachmentDropzone,CustomerSummaryCard,StaffNotificationItem}, src/components/organism/{GroomDayGrid,GroomStatusBoard,PetVaccineVerifyTable,DeskChatThreadList,DeskChatThread,DeskInvoiceSheet}, src/data/schema/frontdesk-grooming-people.ts (8 tables), src/data/seed/frontdesk-grooming-people.ts, src/rules/frontdesk-grooming-people.ts (27 rules), docs/pages/F-30..F-34,F-50..F-59.md, docs/screenshots/F-*/, docs/decisions/_pending/frontdesk-grooming-people.md, docs/ops-manual/en/_pending/frontdesk-grooming-people.md, docs/prompts/_pending/frontdesk-grooming-people.md, supabase/schema.sql + docs/data-model.md + docs/specs.md (regenerated)
codes: F-30, F-31, F-32, F-33, F-34, F-50, F-51, F-52, F-53, F-54, F-55, F-56, F-57, F-60, F-59

## What exists now

- **F-30 Grooming day view** (`/desk/grooming`, replaces the stub): `GroomDayGrid` with groomer columns tinted by `employees.working_hours`, cards labelled per R-G20 with vaccine / payment / warning flags, column menu (move left / right, colour swatches + picker, hide) saved per user + location in `groomer_column_prefs` (R-X62), capacity shading from `capacities.grooming` (R-G21), now line, click a slot to prefill the form, drag a card to reschedule (audit row). Grid hours follow the location hours ±1 h.
- **F-31 Grooming board**: `GroomStatusBoard` kanban by status; drag or "Move to"; transitions mirror the booking lifecycle and PIN-gate cancel / no-show / re-open (R-X63) through `PinApprovalModal`; every move writes `audit_log` (R-J08).
- **F-32 Agenda list**: Figma agenda card as a `DataTable` with the three status flag icons, R-G20 title, line items recomputed from the pricing tables, payment and day total; filters groomer / status / payment; date navigator.
- **F-33 Groom booking form** (`/new`, `/:id/edit`): Groom Bookings fields (date, time, duration computed from package + add-on minutes, reminder, pickup / delivery, customer + New customer round-trip via `?return=`, several pets -> one appointment per pet, groomer with busy hint, package cards priced by each pet's size, add-ons with added minutes, discount % gated by PIN (R-X64), payment method, live invoice block, note ≤ 100 chars + include on invoice). Status = confirmed when vaccines are OK, requested otherwise; customer notification row.
- **F-34 Appointment detail**: Figma booking detail layout (customer card, facts grid, additional information, "Your appointment" lines) with status actions, Edit, Invoice, Message.
- **F-50 Customers / F-51 Add-edit customer / F-52 Customer detail**: list with stat tiles and location toggle; the Customer Details form with `DeskLookupSelect` (+) for title / city / reference / attributes, required-field validation (R-J01), legacy defaults (R-J07), mock attachments; detail with tabs Pets / Hotel / Grooming / Daycare / Invoices / Notes (timeline with pin), soft deactivate behind a PIN (R-X68). Extras live in `customer_profiles`.
- **F-53 Pets / F-54 Add-edit pet / F-55 Pet detail**: list with vaccine standing per pet (`PetVaccineChip`); the Pet Details form with owner selector, weight -> size, breed / colour / temper / vet lookups, registration + microchip (`pet_profiles`), attribute chips (R-A13) and the vaccination table with certificate upload (desk-verified on entry, R-X65); detail with `PetVaccineVerifyTable` (verify / reject with reason / dates / upload) driving `settleVaccineStatus` (R-X60): pet approved + every `pending_vaccines` hotel / daycare booking whose pets are all approved becomes `confirmed`, audit + customer notification.
- **F-56 Vaccine verification queue** (`/desk/vaccines`, the link the seeded staff notification points at): tabs To verify / Blocking a booking / Expired / Rejected / All across pets at the location.
- **F-57 Staff inbox** (`/desk/messages`): two-pane Figma inbox improved per D-018 - filters Open / Mine / Unassigned / Closed, assign to a colleague (`conversation_assignments`, R-X66), close / reopen with system markers, mark read on open, reply notifies the customer; `?customer=` opens or starts that customer's thread.
- **F-60 Staff notifications** (`/desk/notifications`, replaces the F-60 stub path; the TopBar bell links here): unread / all, grouped by day, mark read / all read, open follows the link.
- **F-59 Invoice view & print** (`/desk/invoices/:id`): accepts an invoice id or a booking / appointment / daycare id (preview with watermark), Create invoice numbered from `settings.invoice.next_number` (R-X67), Take payment through the `PaymentProvider` seam (payments row, source payment_status), Print with shell hidden.
- **Tables (8)**: customer_profiles, pet_profiles, appointment_extras, groomer_column_prefs, lookup_values, customer_notes, conversation_assignments, attachments. **Seed**: lookup lists, profile extras, two more Encino groomers + one Westwood groomer with working hours, 18 extra appointments across today / tomorrow / yesterday, customer notes, 3 more threads + assignments, 11 staff notifications, mock attachments. Core groomer rows get `working_hours` via the seed context (noted for the merge).
- **Rules (27)**: R-G16, R-G18 (requested), R-G20, R-G21, R-A11, R-A13, R-B08, R-C06, R-J01, R-J04, R-J05, R-J06, R-J07, R-J08, R-I03, R-I11, R-M08, R-M11 (requested), R-X60..R-X68.
- **Components (14)** with metas: GroomingDateNav, GroomAppointmentCard, GroomStatusBadge, PetVaccineChip, DeskLookupSelect, DeskAttachmentDropzone, CustomerSummaryCard, StaffNotificationItem, GroomDayGrid, GroomStatusBoard, PetVaccineVerifyTable, DeskChatThreadList, DeskChatThread, DeskInvoiceSheet.

## Checks

- `npm run typecheck` and `npm run build` green; Playwright pass over the 15 routes at 360 / 390 / 768 / 1280 / 1920: no console errors, no horizontal overflow (own port 4187 so parallel agents do not collide). Screenshots 390 + 1280 for every code; dark for F-30, F-33, F-52, F-55, F-57, F-59.

## Known gaps

- Daycare day (F-40..F-49) and employees list were in the build-plan range but not in the module assignment; not built.
- Recurring grooms (R-G18), voice / video (R-M11) and real file storage are requested / later. Appointment statuses use the core `APPOINTMENT_STATUS` enum (requested / confirmed / in_progress / done / cancelled / no_show) rather than the hotel lifecycle; transitions and PIN gates mirror it.
- `/desk/notifications` is served here as F-60 while the stub still carries code F-60: the extras module should not rebuild it (or the integrator picks one).

## Integration note (2026-09-18)

- F-58 `/desk/notifications` was retired at merge: extras-manual-website's F-60 (the foundation stub code) owns that path and adds a kind filter. `StaffNotificationItem` was removed with it. Rule ids R-X30..R-X38 of this module were renumbered to R-X60..R-X68 (customer-auth kept R-X30..R-X39).
