# 0019 - QA fixes: 33 of 35 findings from the four QA reports

version: 0.2.1
date: 2026-09-18
prompt: 0007
intent: Apply the findings of the responsive, roles / location / PIN, spec-rules-component-table coverage and end-to-end QA reports (docs/qa/*.md) on main: fix every blocker and major, fix the minors where cheap, record why the rest waits; re-run typecheck, build, the responsive matrix and the failing journeys; document the status per finding.
decision: D-175 (12 px text floor), D-176 (one staff shell, menu per role), D-177 (detail pages honour the pinned location; approvals carry the subject's location), D-178 (quoteLinesOf + ErrorBoundary), D-179 (check-in stays PIN-free, needs Justin), D-180 (MockProvider carries runtime rows over the daily reseed, writes replace arrays), D-181 (capacity rules informational / in_dev), D-182 (QA scripts follow the D-016 brief).
rejected: gating the status menu on `bookings.status` for managers only (would take check-in away from the front desk); collapsing the F-13 legend into a popover (the wrap is the documented graceful degradation and nothing overflows); enforcing daycare capacity inside the customer flow before the daycare desk pages exist (F-40..F-49); adding `checked_in` to PIN_GATED_TRANSITIONS without Justin's decision.
files: src/domain/booking.ts (quoteLinesOf, quoteNotesOf), src/components/atom/StatusBadge/* (new), src/components/atom/Badge/Badge.tsx + meta, src/components/atom/RadioGroup/* (renamed from Radio), src/components/organism/ErrorBoundary/* (new), src/app/App.tsx, src/app/shells.tsx, src/components/template/DesktopShell/*, src/components/organism/FeedbackButton/FeedbackButton.css, src/components/molecule/PageHeader/PageHeader.tsx, src/components/molecule/Toast/*, src/components/organism/RoomTimeline/*, src/components/organism/PinApprovalModal/PinApprovalModal.tsx, src/design/tokens.ts, src/styles/global.css, 30 component CSS files (10 / 11 px -> var(--fs-xs)), src/auth/permissions.ts, src/auth/SessionProvider.tsx, src/tenant/LocationProvider.tsx, src/data/MockProvider.ts, src/data/seed/{core,customer-auth,customer-home-pets,customer-settings-chat,dev-quality,frontdesk-grooming-people}.ts, src/rules/{pricing,operations,vaccines,customer-grooming-daycare,dev-quality}.ts, src/modules/frontdesk-reservations/{BookingDetailPage,TimelinePage,index,specs}.ts(x) + lib/useBookingActions.tsx, src/modules/frontdesk-grooming-people/pages/{CustomerDetailPage,PetDetailPage,InvoicePage,CustomersPage,PetsPage,MessagesPage}.tsx, src/modules/customer-hotel/ReservationDetailPage.tsx, src/modules/customer-home-pets/lib.ts, src/modules/customer-grooming-daycare/{GroomingOrderPage,DaycareBookingPage}.tsx, src/modules/customer-settings-chat/{index,specs}.ts, src/modules/{dev,dev-quality,hub,extras-manual-website,admin-control-panel}/specs.ts, src/modules/settings-rules/index.ts, src/modules/auth/PinLoginPage.tsx, src/modules/dev-quality/a11yScan.ts (layoutScan), scripts/qa-lib.mjs, scripts/qa-responsive.mjs, scripts/screenshots.mjs, scripts/qa-fixes-e2e.mjs (new), docs/qa/fix-status.md (new), docs/qa/{e2e,roles,coverage}-report.md (status per finding), docs/qa/responsive-report.{md,json} (full matrix), docs/qa/e2e-shots-fixes/*, docs/decisions.md, docs/kanban.md, docs/pages/*.md (changelog lines), docs/prompts/0007-qa-fixes.md, docs/screenshots/**
codes: F-12, F-13, F-14, F-01, F-52, F-55, F-56, F-57, F-59, C-10, C-13, C-31..C-36, C-39, C-52..C-55, C-62..C-64, C-78, C-79, A-00, A-20, A-31, A-37, A-40, HUB-02, D-02, D-03, D-05..D-09, D-12, D-13, D-15, D-17..D-19, M-03

## Blocker and majors

- **F-12 white screen for app bookings** (blocker). The customer flow stores `bookings.quote` as `{ hotel, grooming, fee, plan, method }` while the desk stored a full `Quote`; F-12 spread `quote.lines`. `quoteLinesOf()` / `quoteNotesOf()` in `src/domain/booking.ts` read both shapes and are used by F-12, F-59 and C-39. Every route now renders inside `ErrorBoundary` (new organism, in `/dev/components`): a page error shows an EmptyState with Try again / Back to hub instead of unmounting the app.
- **F-56 never refreshed after Verify.** `MockProvider.insert / update` mutated the live array, so a query-less `useTable` kept the same reference and `useMemo` never recomputed. Writes now replace the table array; verified 1 -> 0 rows without reload. The CrudTable `orderBy` workaround is harmless and stays.
- **Links to `/app/chat`** (staff-reply notification, C-55 / C-64 "Message the desk", seed notification) point at `/app/inbox` (the notification deep-links to `/app/inbox/<conversation>`).
- **C-10 / C-13 booking cards** resolve the `grooming_orders` row for an appointment and link to `/app/grooming/orders/<id>` (hub fallback for desk-only appointments) and to `/app/daycare/bookings/<id>`.
- **Pinned staff opened the other location's records by URL.** F-12 compares `booking.location_id`, F-52 the customer's `home_location_id`, F-55 the owner's home location with the pinned location and render the existing not-found state ("This booking belongs to Westwood"). `PinApprovalRequest.locationId` lets `changeStatus` write the approval with the booking's location. The Encino desk seed notification that linked to a Westwood customer moved to `usr_desk_ww`.
- **Manager menu.** Front desk and admin surfaces share one `DesktopShell` with `surfaces: admin + frontdesk + manual`; `hasRole` filters per role, so the manager sees A-27 / A-35 / A-36 / A-37 / A-38 / A-40 / A-42 from `/desk`. The shell title follows the role.
- **12 px text floor.** `--fs-2xs` is 12 px; 30 component CSS files lose their hard-coded 10 / 11 px; count badges (`.iconbtn-badge`, `.sidebar-badge`, `.bottomnav-badge`) grow to 18 px boxes; `code`, `small` and the SVG chart labels are clamped at 12 px; the timeline TODAY tag too.
- **FeedbackButton** sits on the right edge above the SpecChip instead of over the sticky sidebar; the shell content gets 48 px more bottom padding.
- **PageHeader eyebrow** wraps (F-12 at 360 px no longer scrolls horizontally).
- **StatusBadge** is its own atom (`atom/StatusBadge`, meta with the 7 statuses, customer labels, sizes); `Badge.tsx` re-exports it. The `customer` prop now renders `BOOKING_STATUS_CUSTOMER_LABEL` (R-I04).
- **Rules registry**: 25 catalog rows added (R-A09, R-B04, R-D02..D04, R-D08, R-D11, R-D15, R-D18..D20, R-E07, R-G07, R-G09, R-H01, R-H02, R-H06, R-H07, R-I04, R-I10, R-J09, R-J11, R-K07) plus R-X88 (route guard); dead page codes F-20 / F-40 repointed; `implementedIn` filled for R-A01, R-B05, R-B07, R-B11, R-M05, R-X01, R-X02; R-F06 / R-G15 retitled as positive rules; R-E13 `in_dev`, R-E10..E12 pointers corrected. Every `spec.rules` id resolves.
- **Specs**: no empty `rules` or `data` left (tooling pages cite R-X80 / R-X81 / R-M04 and `rules`, `page_layouts`, `qa_runs`, `audit_log`; C-78 / C-79 cite R-X39 / R-M05; HUB-02 cites R-X88; A-40 cites R-X80 / R-X41; A-20 lists R-D09 / R-D10 / R-D12). `spec.roles` equals `route.roles` everywhere.

## Minors

Customers / Pets location toggle only for roles that may switch; F-14 board uses the DESK role set; `bookings.status` is granted to the front desk and drives the status menu (PIN gate unchanged); view-as pins to the demo user's location; PIN login names the employee without a login; `viewAs` is exposed only for the super admin; `atom/Radio` -> `atom/RadioGroup`; four empty tables get one demo row each; chat seed timestamps are relative to seeding time and the seeded staff reply no longer quotes a price; timeline "Checked in" on a room-less booking opens the picker with "Assign & check in" and checks in; the popover flips above the block near the bottom; toasts sit above the bottom nav and cap at 2 on phones; the mock database carries runtime rows across the daily reseed.

## QA tooling

`scripts/qa-lib.mjs` signs in per surface (customer routes as `usr_customer`, dev mode off), fills `:slug` / `:conversationId`, and seeds the hotel / grooming / daycare drafts so wizard steps render themselves. `qa-responsive.mjs` adds `layoutScan` (text under 12 px, fixed element over a stuck sticky element, blank page) and redirect detection; the full matrix is committed for D-12. `scripts/qa-fixes-e2e.mjs` re-verifies the fixed journeys (11 checks, all pass; shots in `docs/qa/e2e-shots-fixes/`).

## Deferred

- Check-in PIN gate (finding 33): needs Justin (D-179).
- F-13 legend collapse below 600 px (part of finding 6): documented graceful degradation, no overflow.
- Daycare capacity enforcement in booking flows (finding 20): with the daycare desk pages.
