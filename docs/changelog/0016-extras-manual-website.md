# 0016 - public website, ops manual, front desk extras

version: 0.2.0 (pending)
date: 2026-09-18
prompt: 0005
intent: Give Petrock a real public website (P-01..P-12) driven by the same tables as the app, a business operations manual per role with live data blocks and screenshot placeholders (M-01..M-03 + chapters M-10..M-27), and the front desk extras the build plan reserved: staff notifications (F-60), messages inbox with website inquiries (F-61), reports overview / revenue / occupancy (F-62..F-64), reviews moderation (F-65), education (F-66), walking (F-67), tasks & checklists (F-68).
decision: docs/decisions/_pending/extras-manual-website.md (website lives under /site/<page> beside the P-00 placeholder; manual chapters are markdown with front matter and one route each; reports respect location scope and gate money behind reports.financial; only published reviews reach the website; new tables site_faqs, site_inquiries, training_completions, walks, tasks; owner rules R-X70..R-X78).
rejected: replacing P-00 at /site from this module (would collide with the public module's route; the integrator points /site at P-01 in one place); a single parameterised /manual/:slug route only (every chapter gets its own code, spec, screenshots and page doc instead); a Tailwind-free chart library (ReportBarChart is a small SVG organism with one series, one axis, hover and a table view); photography in the repo (gradient Art blocks until brand photos exist).
files: src/modules/extras-manual-website/**, src/components/atom/ReviewStars, src/components/molecule/{ReviewListItem,StaffNotificationRow,ManualCallout,ManualToc,ManualChapterCard,SiteHero}, src/components/organism/{LiveBlock,DeskChatThread,ReportBarChart}, src/components/template/SiteLayout, src/data/schema/extras-manual-website.ts, src/data/seed/extras-manual-website.ts, src/rules/extras-manual-website.ts, docs/ops-manual/README.md, docs/ops-manual/en/*.md, docs/ops-manual/en/_pending/*, docs/pages/{P-01..P-12,M-01..M-03,M-10..M-27,F-60..F-68}.md, docs/screenshots/{same codes}/, docs/decisions/_pending/extras-manual-website.md, supabase/schema.sql + docs/data-model.md (regenerated)
codes: P-01, P-02, P-03, P-04, P-05, P-06, P-07, P-08, P-09, P-10, P-11, P-12, M-01, M-02, M-03, M-10, M-11, M-12, M-13, M-14, M-15, M-16, M-17, M-18, M-19, M-20, M-21, M-22, M-23, M-24, M-25, M-26, M-27, F-60, F-61, F-62, F-63, F-64, F-65, F-66, F-67, F-68

## What exists now

- **Public website** (`/#/site/home` … `/#/site/about`, surface `public`, roles everyone): `SiteLayout` (sticky header, drawer nav under 960 px, theme toggle, Book now CTA, footer from `locations`), `SiteHero`. Home with service teasers, why Petrock, locations, published reviews, how it works; Hotel (room types, from-rates, discounts, vaccines); Grooming & Spa (packages by size, add-ons); Daycare (price items, thresholds, capacity); Pricing (every table, live); Locations (hours collapsed into ranges, open-now, capacities, directions); Reviews (published only, average, distribution, location filter); Vaccines & policies (vaccine types, customer status labels, fee / prepay copy); Book now (hand-off into the app); Contact (form -> `site_inquiries`); FAQ (`site_faqs` by topic with search); About (values, team from active employees). Every price is computed from the tables (R-X71); nothing is typed.
- **Ops manual** (`/#/manual`, surface `manual`, staff roles): `manualIndex.ts` globs `docs/ops-manual/en/*.md`; front matter gives title, code, roles, part, version, updated, summary, rules; the body is split into segments: markdown, `{{directive}}` -> `LiveBlock`, `[screenshot: CODE — caption]` -> `Figure` (real capture when `docs/screenshots/CODE/` exists), `> NOTE:` / `> TIP:` / `> WARNING:` / `> DECISION NEEDED:` / `> IN PERSON:` / `> ONLINE:` -> `ManualCallout`. One route + spec per chapter (`chapterSpec()` derives tables from directives). Cover M-01 with the reading path for the current role, role x chapter matrix and part grids; M-02 fallback; M-03 pending (decisions, screens to capture, drafts from `_pending/`). Lesson buttons write `training_completions` (R-X74). 18 chapters: 10 welcome · 11 front desk daily ops · 12 check-in/out · 13 vaccine verification · 14 reservations & lifecycle · 15 grooming agenda · 16 daycare day · 17 PIN approvals · 18 payments · 19 messages, notifications & reviews · 20 manager duties · 21 owner settings & pricing · 22 roles, permissions & locations · 23 reports · 24 feedback & rules registry · 25 glossary & tables · 26 website & app · 27 walks, tasks & education.
- **LiveBlock directives**: `table:<name>`, `tables`, `roles`, `permissions:<role>`, `routes:<surface>`, `locations`, `capacities`, `pricing:rooms|grooming|addons|daycare|discounts|fees`, `statuses`, `vaccines`, `rules:<category>`, `rule:<id>`, `stats`, `demo-users`; unknown directives explain themselves (R-X73).
- **Front desk extras** (surface `frontdesk`): F-60 notifications (unread/all, kind filter, mark read on open, bell count; `StaffNotificationRow`, `relativeTime`), F-61 messages (conversation list, `DeskChatThread`, website inquiries tab with seen / replied / closed), F-62 reports overview (period 7/30/90, KPIs, `ReportBarChart`, attention list, bookings by status), F-63 revenue (owner: by day / service / method, fee + tax, outstanding, CSV export), F-64 occupancy (rooms per night vs capacity, per room type meters), F-65 reviews moderation (reviews.jpg: tabs, rating filter, Approve / Archive with `ReviewListItem`), F-66 education (my progress + team matrix, sign-off), F-67 walking (walk log: plan / start / finish / skip), F-68 tasks & checklists (add drawer, complete, start, PIN-gated delete via `PinApprovalModal`).
- **Tables (5)**: `site_faqs`, `site_inquiries` (location), `training_completions` (location), `walks` (location), `tasks` (location) with seed rows. `npm run sql` regenerated `supabase/schema.sql` and `docs/data-model.md`.
- **Rules (14)**: R-M07, R-M08, R-M11, R-M13, R-L07 from the design catalogue; R-X70..R-X78 owner rules first registered here (10 implemented, 3 requested, 1 in the registry as requested from the design).
- **Components (11)**: ReviewStars · ReviewListItem, StaffNotificationRow, ManualCallout, ManualToc, ManualChapterCard, SiteHero · LiveBlock, DeskChatThread, ReportBarChart · SiteLayout. Each with a meta and usages in /dev/components.

## Verification

- `npm run typecheck` and `npm run build` green.
- `node scripts/screenshots.mjs --smoke` over the 42 module routes: no console errors.
- Responsive script (scratchpad, Playwright against `vite preview`) at 360 / 390 / 768 / 1280 / 1920 over every route: no page-level horizontal overflow after fixing the hidden site nav that inflated `scrollWidth`; wide tables scroll inside their block.
- Screenshots 390 + 1280 for all 42 codes; dark variants for P-01, P-05, M-01, M-11, F-61, F-62, F-66.

## Known gaps

- P-00 still answers `/site`; P-01 lives at `/site/home` until the integrator points `/site` at it (one-line change in `src/modules/public/index.ts` or a redirect).
- Screenshot placeholders in the chapters for pages other modules build (F-10, F-11, F-12, F-20, F-31, F-40, F-52, F-55, A-20, A-30, A-35, A-36, A-37, A-41, C-20, C-30) render as dashed boxes until those captures exist; M-03 lists them.
- Chapters are English only; a `docs/ops-manual/es/` mirror would follow the same file names.
- No photography: `Art` blocks are brand-gradient placeholders.
- Contact-form notifications, push for chat replies and the daycare walk billing link are not wired (R-M13, R-F05).
- Rule ids R-X70..R-X78 were chosen high to avoid clashing with other modules' new owner rules; the integrator resolves any collision.
