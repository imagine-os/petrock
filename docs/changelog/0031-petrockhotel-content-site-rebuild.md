# 0031 - Public website rebuilt on petrockhotel.com content: media hero, gallery P-13, pricing seed from the live site

version: 0.3.0
date: 2026-09-20
prompt: 0017
intent: Put the client's own material on the public website, hero first (Justin: "for the public website please scrape the info and images and video from petrockhotel.com ... especially the hero section should integrate that stuff well"): crawl the live site into the repo, build a media hero from their background video over their lobby photo, replace the invented copy and the gradient `Art` placeholders with their words and their 50 photos across P-01..P-04 and P-12, add a gallery page for the two Squarespace galleries, and - because D-187 makes the site the first source for prices - adopt the live price lists into the pricing tables.
decision: D-224..D-230 (scrape is the content source set, site-images pipeline + SitePhoto, media hero from the client's own video, P-13 gallery, training informational only, pricing seed adopted per D-187, legal entity Petrock Holdings LLC); D-194, D-195, D-197, D-206 further implemented; D-214 lifted for the public site (real photos now exist).
rejected: training prices in the UI without a table (D-228: the copy and a call-to-consult CTA only; the numbers live in the adoption doc); hardcoding any price on the site pages (every number still comes from the tables through R-X71); committing only the originals and generating derivatives in CI (`npm run build` does not run sharp, so the 152 derivatives are committed and sharp is a devDependency); autoplaying the video on phones or under `prefers-reduced-motion` (poster only below 700 px, and a visible play / pause control either way); adopting the "Holidays: please call" per-day prices or the cat grooming prices without a schema column for them; renaming the season ids (kept `sea_summer` / `sea_winter` so existing references such as the A-24 audit-log row id still resolve).
files: docs/reference/petrockhotel-scrape/{INVENTORY.md,all_images.json,download_results.json,pages/*.md,images/*}, docs/reference/pricing-adoption-2026-09-20.md, docs/reference/petrockhotel-site.md, scripts/site-images.mjs, public/site/img/*, src/components/molecule/SitePhoto/{SitePhoto.tsx,SitePhoto.css,SitePhoto.meta.ts}, src/components/molecule/SiteHero/{SiteHero.tsx,SiteHero.css,SiteHero.meta.ts}, src/components/atom/Icon/Icon.tsx, src/components/template/SiteLayout/{SiteLayout.tsx,SiteLayout.css}, src/modules/extras-manual-website/{index.ts,specs.ts}, src/modules/extras-manual-website/site/{SiteHome.tsx,SiteServices.tsx,SiteInfo.tsx,SiteLocations.tsx,SiteGallery.tsx,siteImages.tsx,siteImages.generated.ts,strings.ts,siteBits.tsx,teasers.ts,site.css}, src/data/seed/{core.ts,admin-control-panel.ts}, src/data/MockProvider.ts, src/pricing/engine.ts, src/rules/pricing.ts, src/modules/admin-control-panel/specs.ts, src/tenant/locations.ts, scripts/test-pricing.mjs, package.json, package-lock.json, docs/prompts/0017-petrockhotel-content.md, docs/changelog/0031-petrockhotel-content-site-rebuild.md, docs/decisions.md, docs/kanban.md, docs/pages/{P-01,P-02,P-03,P-04,P-12,P-13}.md, docs/reference/surfaces.md, docs/platform-principles.md, docs/specs.md, docs/screenshots/routes.json, docs/screenshots/{P-01,P-02,P-03,P-04,P-12,P-13}/{390,1280}.jpg, docs/screenshots/P-01/{390,1280,3840}-dark.jpg, docs/screenshots/P-01/3840.jpg, docs/screenshots/{P-01,P-10}/thumb*.jpg
codes: P-01, P-02, P-03, P-04, P-12, P-13, A-23 (spec text); seed affects C-30..C-37, C-50..C-54, C-60..C-63, F-1x, A-20..A-25, P-05 prices

## What changed

- **Source set (the crawl)**: petrockhotel.com became reachable on 2026-09-20 and was crawled in full into `docs/reference/petrockhotel-scrape/` - `INVENTORY.md` (what exists, with the crawl date), one markdown file per page under `pages/`, `all_images.json` and `download_results.json`, the 50 original images and the hero video reference. This is now **the** content source set for the public website (D-224) and it supersedes Part A of `docs/reference/petrockhotel-site.md` where the two differ; that file keeps its history and gains a short "0a. Update 2026-09-20" section pointing here. Re-crawl when the client changes the site and record the date in `INVENTORY.md`.
- **Image pipeline + SitePhoto**: new `npm run site:images` (`scripts/site-images.mjs`, sharp as a devDependency) turns the 50 originals into 152 committed derivatives in `public/site/img/` - WebP at 480 / 960 / 1600 / 2560 (never upscaled), a 1600 JPEG fallback, every file <= 450 KB, plus the logo PNG - and writes `siteImages.generated.ts` with each slug's candidate widths and source pixel size. Pages never touch a path: the new `SitePhoto` molecule (`src/components/molecule/SitePhoto/`) renders `<picture>` with the srcset, intrinsic width / height so nothing shifts, and `loading="lazy"` unless `priority`. Alt text is a translated string per slug (`extras-manual-website.photo.<slug>`, 47 keys). Derivatives are committed because CI only runs `npm run build`; never hand-edit them (D-225).
- **Hero (SiteHero `tone="media"`)**: the hero now layers the client's own background video (`youtube-nocookie`, id `htG1NJsfp24`) over their real lobby photo under a brand scrim measured at 7:1 average contrast for the white text (>= 4.5:1 everywhere). The video mounts only above 700 px, only with `prefers-reduced-motion: no-preference`, and never under `navigator.webdriver`; the poster photo is always rendered underneath, so phones, reduced-motion users, a blocked embed and every screenshot capture all show a real photo. A visible 44 px play / pause `IconButton` (new `play` / `pause` icons in the Icon atom) toggles it - nothing is hover-only. Hero copy is the eyebrow, `company.tagline`, the service strip (Hotel · Grooming & Spa · Day care · Training), the real 2011 lead, and Book now plus one `tel:` button per location read from `locations` (D-226). New `media`, `subhead` and `tone="media"` props are documented in `SiteHero.meta.ts`.
- **Site copy and pages**: P-01 (hero, services in the client's words with live "from" teasers, **Training & Fitness**, why Petrock, photo strip, locations, reviews, how it works, CTA), P-02 hotel (their inclusion lists, real room photos), P-03 Grooming & Spa (their package contents, salon photos), P-04 daycare (the "Play" copy, the real yard) and P-12 about (established 2011, the story, the legal entity) all read the client's wording. **New P-13 `Website: Gallery`** at `/site/gallery` rebuilds their two Squarespace galleries as one page - 28 hotel photos and 12 spa photos in a responsive grid, each tile a 44 px button, with a `Modal` lightbox (previous / next arrows, Escape, an "n of N" counter and the photo's alt text as the caption); it sits in `SITE_NAV` after Reviews (D-227). Training is informational only: real copy and a call-to-consult `tel:` CTA, no prices in the UI until a table exists (D-228).
- **Pricing seed adopted from the live site** (D-229, D-187; full before / after in `docs/reference/pricing-adoption-2026-09-20.md`):

  | Area | Now seeded |
  | --- | --- |
  | Penthouse (Mon-Thu / Fri-Sun) | base **$135 / $150**, seasonal **$145 / $160**, holiday **$165 / $175** |
  | Suite (Mon-Thu / Fri-Sun) | base **$100 / $110**, seasonal **$110 / $120**, holiday **$120 / $130** |
  | Seasons | two distinct bands: Seasonal (Easter - Labor Day, 2026: Apr 5 - Sep 7) and Holiday (Thanksgiving - New Year's, 2026: Nov 26 - Jan 1), ids `sea_summer` / `sea_winter` kept |
  | Daycare | Full $45 / Half $35 / Play hour $15 with `threshold_hours` **5** (R-F01 resolved, no longer a guess) |
  | Grooming | Gold 55 / 70 / 85 / 100 / 140, Platinum 70 / 85 / 100 / 120 / 155, Diamond 95 / 110 / 130 / 150 / 210 (S / M / L / XL / Giant), inclusions quoted from the spa menu |
  | Add-ons | 5 repriced, 7 added (ear clean, brush teeth, face trim, face trim with sanitary, foxtail removal, non-anesthetic dental, transportation) - 18 rows |
  | Daycare packages | prepaid 7 / 14 / 21 days at 5 / 7.5 / 10 % seeded as `prepay` discounts (not yet applied by `quoteDaycare` - backlog) |

  `SEED_VERSION` 3 -> 4 so existing localStorage databases reseed. `scripts/test-pricing.mjs` fixtures and expectations were recomputed and pass. Not adopted, by decision: the per-holiday "please call" prices, cat grooming, the large-dog accommodation fee and the dematting surcharge (no schema column or no published number); ranges are seeded at their low end with the range in `notes`. Hotel long-stay discounts, the 2 % tax and the 3.89 % card fee are not on the site and were left as they were.
- **Company facts**: the legal entity is **Petrock Holdings LLC** (from the site's privacy policy), now `company.legalName` in `src/tenant/locations.ts`, read by the site footer and the About page (D-230). Every address, phone, SMS number, email and hours block already matched the live site and was left alone.
- **Strings**: 130 new `{ en, es }` keys - 83 copy keys in `src/modules/extras-manual-website/site/strings.ts` and 47 photo alt keys from `siteImages.tsx` - with real Spanish, not placeholders. No price and no hard-coded English in the JSX (D-206).
- **Specs and actions**: P-01..P-04, P-12 and P-13 specs rewritten (purpose, layout, logic, components incl. `SitePhoto`, `checkedAt` to 3840). P-01 declares 8 actions (`site.book`, `site.callLocation`, `site.hero.playVideo`, `site.hero.pauseVideo`, `site.openService`, `site.openGallery`, `site.openLocations`, `site.openReviews`), P-13 declares 5 (`site.gallery.open` / `.next` / `.prev` / `.close`, `site.book`), P-02 four, P-03 two and P-12 four (D-197). A-23's purpose string now says the daycare cut-off is 5 h from petrockhotel.com rather than "6 h chosen".
- **Docs**: prompt 0017, this entry, decisions D-224..D-230, kanban (Done card, the D-187 verification card moved to Done, the "Awaiting Justin" card edited, four Backlog cards added), `docs/pages/P-01.md` rewritten and P-02 / P-03 / P-04 / P-12 updated, new `docs/pages/P-13.md`, `docs/reference/pricing-adoption-2026-09-20.md`, the 0a section in `docs/reference/petrockhotel-site.md`, `docs/reference/surfaces.md` (the `/site/gallery` route, the new action ids, the `npm run site:images` row, last full pass 2026-09-20), `docs/platform-principles.md` today-lines for P-07 and P-13, regenerated `docs/specs.md`, and screenshots for P-01..P-04, P-12, P-13 at 390 / 1280 plus P-01 dark and 3840.
- **Hub thumbnails**: `npm run thumbs -- --codes=P-01,P-10` regenerated the public-website cards on HUB-01 (`docs/screenshots/{P-01,P-10}/thumb[-dark].jpg`), which still showed the old gradient hero; they now show the media hero with the client's lobby photo (D-218).
- **Version** 0.3.0 in `package.json` / `package-lock.json`.
