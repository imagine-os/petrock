# Pending changelog drafts

Module agents drop one file here per module (`<module>.md`, same header lines as a numbered entry: version, date, prompt, intent, decision, rejected, files, codes). The integrator merges them into the next numbered `docs/changelog/NNNN-*.md` and deletes the drafts. Never renumber existing entries.
