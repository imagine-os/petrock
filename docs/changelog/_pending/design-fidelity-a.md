# pending - Figma fidelity part (a): tokens, fonts, icons, core components

version: 0.2.0
date: 2026-09-18
prompt: 0010
intent: Land the Figma look at the design-system level (Justin: "the icons, the colors, especially the homepage on mobile ... fix this at the design system level") so every page inherits it: Open Sans / Be Vietnam Pro via @fontsource, the audited palette / type / radii / shadows / sizes in `tokens.ts`, the 29 exported Figma SVGs plus the redrawn bottom-nav and services glyphs behind `<Icon name>`, and the shared components restyled per `docs/design/fidelity-audit.md` section 3.
decision: D-185 (Open Sans + Be Vietnam Pro, implemented, needs Justin), D-186..D-188 appended as proposed defaults for parts (b) / (c). Shell-scoped field / card skins (custom properties set by PhoneShell / DesktopShell) instead of per-page overrides. `PhonePageHeader` is now an alias of `CustomerScreenHeader` (dedupe done). `PageSpec.tone` ('home' | 'list' | 'form') drives the customer screen background.
rejected: Google Fonts link (fontsource works offline through the proxy and ships with the bundle); rescaling the export path data to a 24 box (arc flags break; each icon keeps its own square viewBox instead); removing the Chat tab from the bottom nav here (module-owned route, flagged for part b); dropping the location switcher from the top bar (binding rule).
files: package.json, index.html, src/main.tsx, src/styles/global.css, src/design/tokens.ts, src/styles/tokens.css (generated), src/specs/types.ts, public/brand/petrock-logo*.png, src/components/atom/Icon/{Icon.tsx,figmaIcons.ts,Icon.meta.ts}, atom/{Button,Input,Select,Checkbox,Badge,StatusBadge,Avatar,IconButton}, molecule/{BottomNav,CustomerScreenHeader,PhonePageHeader,Card,Section,Tabs,SegmentedControl,Stepper,StatTile,HomeServiceTile,PetAvatarCard,CustomerBookingCard,AccountMenuRow,NotificationRow}, organism/{Modal,DataTable,Sidebar,TopBar}, template/{PhoneShell,DesktopShell}, docs/design/tokens-draft.md, docs/decisions.md, docs/kanban.md, docs/prompts/0010-design-fidelity-part-a.md, docs/screenshots/{C-10,C-13,C-30,C-70,C-80,F-01,F-10,HUB-01}
codes: C-10, C-13, C-30, C-70, C-80, F-01, F-10, HUB-01, D-02

## What changed

- **Fonts**: `@fontsource/open-sans` 400/600/700 + `@fontsource/be-vietnam-pro` 400/500/600 imported in `main.tsx`; Google Inter link removed; `Open Sans Fallback` / `Be Vietnam Pro Fallback` metric overrides in `global.css`; headings use `--lh-title` 1.35 and no negative tracking. Verified in Playwright (`document.fonts`: Open Sans 400/600/700 loaded).
- **Tokens**: every row of the audit delta table (`docs/design/tokens-draft.md` section 7): `#F4F0FF` desk canvas, three phone tones, `#181818` text / `#000` titles, `#808080` labels, `#DFDFDF` / `#D8DADE` / `#F1F1F1` / `#D4D4D4` borders, `#9D67EF` 2 px selected border and icon purple, `#FD866E` coral, `#27B14E` / `#FF7A00` / `#FF6868` statuses, `#BF0000` badge dot, purple table head, mint completed badge, r4 inputs, desk / pet shadows, 71 / 80 / 243 / 48 / 40 / 28 / 44 / 49 sizes. Dark theme re-derived; `sunset` still resolves.
- **Icons**: `ICON_SVGS` registry (`figmaIcons.ts`, 32 entries) rendered by the same `<Icon>`: 20 from the exports (`row-chevron`, `calendar-filled`, `info-filled`, `tag`, `lock-filled`, `logout-filled`, `message-filled`, `moon-filled`, `question-filled`, `settings-filled`, `shield-filled`, `trash-filled`, `user-filled` x3, `users-filled`, `vet`, `clipper`, `medal`, `category`) and 12 redrawn (`nav-home`, `nav-ticket`, `nav-paw` multi-colour disc, `nav-settings`, `ticket`, `svc-hotel`, `svc-spa`, `svc-daycare`, `svc-inhome`, `chat-notification`, `clock-filled`, `paw-filled`). Default outline stroke 1.5. `BottomNav.NAV_GLYPH`, `HomeServiceTile.TILE_ICON`, `AccountMenuRow.FILLED_ICON` and `NOTIFICATION_KIND_ICON` map the generic names pages already pass to the Figma glyphs, so C-10 / C-70 / C-80 pick them up without edits.
- **Components** (audit section 3): BottomNav purple 71 px unlabelled with dot badge; CustomerScreenHeader 40 px back / 20-600 black title / right text action / `#B6B6B6` rule (PhonePageHeader aliases it); PhoneShell tone + field skin; Button 48 / sm 40-12-700 / link underlined; Input / Select / Textarea 12 px grey label, 48 tall, shell skin, `size="xs"` 28; Checkbox Material 22-24 r3; Badge `variant` fill / pill / text + `completed` tone, StatusBadge same; Avatar `ring`; Card border-only on mobile, desk shadow pair; Section grey 400 labels on phone; Tabs `variant="split"`; SegmentedControl filled active; Stepper filled 27 px pointers; Modal `size="alert"` (270 px, split footer); StatTile desk card + display value; HomeServiceTile 76 px r8 no shadow; PetAvatarCard 60 px avatar, `shadow-pet`, 2 px selected border, `filled`; CustomerBookingCard 60 px line icon, 2 px border, text status, filled calendar / clock; AccountMenuRow 68 px filled purple / coral icons, display label, no chevron; NotificationRow 44 px disc, display title; DataTable purple 49 px head, 44 px rows, `tone` per column, desk shadow; Sidebar 243 borderless, logo 145x44, uppercase grey groups, purple active text, Log Out button, codes dev-only; TopBar 80 px with search, location, New booking, chat, Help, bell, user (theme / brand moved into the user menu); DesktopShell `#F4F0FF` canvas.
- **Docs**: D-185..D-188 appended; tokens-draft section 7; kanban; prompt 0010; screenshots for the eight codes at 390 / 1280 light + dark.
- **Verification**: `npm run build` green; `npm run qa:responsive` 181 routes x 360/390/768/1280/1920 x light + dark = 1810 cells, **0 failing** (a11y findings 8730 -> 8356); fonts confirmed loaded in Playwright. Commits: `e42049e` tokens + fonts, `b808cd4` icons, `739be3e` components + shells, plus this docs / screenshots commit.

## For the screen agents (parts b / c)

New token names: `--font-display`, `--fs-lg-2`, `--fs-xl-2`, `--lh-title`, `--ls-body`, `--ls-button`, `--color-bg-phone-list`, `--color-bg-phone-form`, `--color-band`, `--color-title`, `--color-label`, `--color-text-secondary`, `--color-text-option`, `--color-border-card`, `--color-border-input`, `--color-border-bar`, `--color-border-row`, `--color-border-track`, `--color-surface-tint-band`, `--color-icon-primary`, `--color-icon-header`, `--color-icon-muted`, `--color-accent-coral(-soft)`, `--color-danger-soft`, `--color-badge`, `--color-success-text`, `--color-success-strong`, `--color-completed(-bg)`, `--color-table-head(-text)`, `--color-table-zebra`, `--r-input`, `--r-cb`, `--shadow-desk`, `--shadow-pet`, `--h-thead`, `--h-control-xs`. New props: `spec.tone`, `CustomerScreenHeader.rule` / `backTo: number`, `Badge` / `StatusBadge.variant`, `Checkbox.size`, `Avatar.ring`, `PetAvatarCard.filled`, `AccountMenuRow.chevron`, `Tabs variant="split"`, `Modal size="alert"`, `Input` / `Select size="xs"`, `DataTable column.tone`, `Sidebar.showCodes`, `TopBar.searchTo / newBookingTo / chatTo / helpTo`. `--r-lg` and grey `--shadow-sm` should no longer be used on mobile tiles / cards. The Chat bottom-nav entry (`customer-settings-chat/index.ts`) still renders as a fifth tab until part (b) moves chat under the Notification tabs (D-186 / audit section 3). The `svc-*` services glyphs are provisional (vectorised from `Services.png`); real ones need the Design System export after the Figma quota resets.
