# Pending decisions - admin-control-panel (A-01..A-49)

Choices the module builder made where `docs/figma/open-questions.md` left something open. The integrator assigns D-numbers; rows marked **needs Justin** change pricing or scope and must be confirmed.

| id | decision | rationale | source question | status |
| --- | --- | --- | --- | --- |
| D-pending-ACP-01 | One Settings area for the owner (A-1x..A-4x under the admin surface); front desk pages read the same tables and never carry their own settings screens. Tab names from 12-14.pdf (Spa / Hotel & Daycare / Employee setup) are replaced by task pages: Pricing (A-27 with sub-pages), Services (A-26), Employees (A-30). | D-002 makes Control Panel owner-only; one source avoids two copies of pricing. | Q94 | proposed |
| D-pending-ACP-02 | Each modal gets its own title (Company profile, Location details, Employee) instead of the shared "Name And Contact Details". | Copy error in 2/3/15.pdf. | Q95 | proposed |
| D-pending-ACP-03 | Boarding settings keep one "book out whole room by default" toggle; the duplicate in 4.pdf is dropped until Justin names the second option. | Duplicate field. | Q96 | proposed |
| D-pending-ACP-04 | Invoice settings: "Boarding Hours" section label ignored; Payment Processing Option 1-3 replaced by the PaymentProvider seam card on A-28 (mock now, Stripe later). | Placeholders in 9.pdf; Stripe is on the kanban. | Q97 | proposed |
| D-pending-ACP-05 | Package calendar minutes seeded per size (Gold 60/60/60/90/90, Platinum 75/75/90/105/120, Diamond 90/105/120/135/150); Platinum prices from the Spa 12.7 card; Diamond prices placeholders. All editable on A-24. | 12.pdf time cells contain dollar values. | Q98 | **needs Justin** (minutes and Diamond prices) |
| D-pending-ACP-06 | Add-on "+" = `starting_at` flag (final price depends on coat condition); the duplicate Color/Highlights row is seeded once at $15; "special employee" is a free-text `employee_type` on the add-on, editable on A-25. | Two entries and an undefined taxonomy. | Q99 | **needs Justin** (Color/Highlights price) |
| D-pending-ACP-07 | Capacity table's second "LOS ANGELES" column treated as a typo; capacities are per location and editable on A-11; a third location is added through A-41 when needed. | No third location exists. | Q100 | proposed |
| D-pending-ACP-08 | Seasonal rates are date-range configurable (seasons table with holiday flag, A-20); holidays per location are configurable on A-11 and exclude long-stay discounts. | R-D06, R-E15. | Q101 | proposed |
| D-pending-ACP-09 | Settings cards use a drawer (AdminRecordDrawer) for add / edit; delete exists and is PIN-gated (R-X42); no inline table editing. | One consistent form; deletes audited. | Q102 | proposed |
| D-pending-ACP-10 | "General User Security Setting" is per role: the permissions matrix (A-31) edits the `permissions` table; the manager PIN is separate (approvals) and its length stays 4-6 digits (D-019). | 10.pdf lists permissions without a subject; roles are the unit elsewhere. | Q103, Q104 | proposed |
| D-pending-ACP-11 | Default location hours Mon-Fri 7:00-19:00, Sat-Sun 9:00-17:30 (R-K02); the 07:45 PM dropdown defaults in the PDFs are placeholders. | Consistent with 1.pdf / front desk-7. | Q105 | proposed |
| D-pending-ACP-12 | Providers: email None / Gmail / SMTP, SMS None / Twilio / Petlinx (legacy), push None / FCM / APNs; all stubs with test-send logging only. | Platform integrations replace PetLinx; nothing sends until Company-OS. | Q106 | proposed |
| D-pending-ACP-13 | Employee record = one `employees` row with operational fields (job, department, status, colour, groomer / handler flags, PIN, working hours JSON) linked to a `users` row for login and role; HR tabs (payroll, performance, documents) are out of scope. | D-005 employees = user management. | Q108, Q109 | proposed |
| D-pending-ACP-14 | Department is a short fixed list (Operations, Receptionist, Groom, Take Care, Sales); "On leave" is set manually; phone default +1. | Design list mixed titles and departments. | Q110 | proposed |
| D-pending-ACP-15 | Review moderation: pending -> published or archived; a published review can go back to pending or be archived (unpublish). Tags come from the customer app; ratings one decimal. | R-M12; answers Q111. | Q111 | proposed |
| D-pending-ACP-16 | Reports use booked value (totals of confirmed / in-house / completed records) rather than cash received; the dashboard revenue tile uses paid payments. CSV export per report tab. | Cash vs booked both useful; labelled on each page. | Q75 | **needs Justin** (which KPI set on the dashboard) |
| D-pending-ACP-17 | Per-role side menus are stored in `roles.menu` (category -> visible), edited on A-32; Overview cannot be hidden. DesktopShell reads it after integration (shared file). | D-014. | - | proposed |
| D-pending-ACP-18 | New owner rules for this module use ids R-X40..R-X49 (the A-4x range) to avoid clashes with other module builders' R-Xnn ids. | Parallel builders cannot coordinate ids. | - | proposed |
