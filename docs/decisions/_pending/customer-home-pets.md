# Pending decisions - customer-home-pets (C-10..C-21)

Choices made while building the module where the Figma export conflicted or was silent. The integrator turns these into `D-0xx` rows in `docs/decisions.md`; rows marked **needs Justin** should be confirmed before launch. Source question numbers refer to `docs/figma/open-questions.md`.

| Id (placeholder) | Decision | Rationale | Source question | Status |
|---|---|---|---|---|
| D-CHP-01 | **Add pet wizard = 5 steps in one stepper**: Basics, Size (lbs), Personality & feeding, Vet & emergency, Vaccines. Socialisation / personality / treats live only on step 3. | Figma had a 2-step stepper but three screens and duplicated questions; five short steps keep each screen one scroll on a phone. | 23 | proposed |
| D-CHP-02 | **Personality is a radio group; meals per day is slot based** (AM, PM, AM & PM, AM / Mid Day / PM, Free feeding), and feeding instruction fields show only for the chosen slots. | Radio matches the Figma design; slots (not a count) drive the per-slot instructions the desk needs. | 24 | proposed |
| D-CHP-03 | **Weight in lbs everywhere**, numeric input with an lbs suffix; size band S <20, M <40, L <70, XL <100, Giant 100+ computed and stored on save. | Room fit and grooming tiers are in lbs (D-019); the Kg profile is a UI-kit screen. | 4, 28 | proposed (bands **need Justin**, per D-019) |
| D-CHP-04 | **Type defaults to Dog; Cat / Other stay selectable but all copy says dog.** | The hotel is dog-only in practice; the field stays open so no data model change is needed later. | 25 | **needs Justin** |
| D-CHP-05 | **My Pets grid and a pet-only Pet profile are in scope; the UI-kit Reminder / Notes list and owner e-mail / phone on the pet form are not.** | Reminders and notes duplicate notifications and bookings; owner fields belong to the account. | 26, 27 | proposed |
| D-CHP-06 | **One proof per vaccine record** (per-vaccine uploads), accepted .jpg .png .pdf; .gif not accepted until confirmed. Proof is required to submit a record. | Staff verify vaccine by vaccine; a bulk upload cannot be matched to a type. | 32, 33 | proposed (.gif **needs Justin**) |
| D-CHP-07 | **Vaccine status per record derives from the record and today**: missing / pending (submitted) / verified / expired (expires_on before today even if verified) / rejected. Pet-level approval: Approved (staff) · Pending verification · Needs more details (a required vaccine missing or skipped) · Vaccine expired. | Answers which states exist and who sets them: customers upload, the front desk verifies and approves. | 34 | proposed |
| D-CHP-08 | **The pet-card date on Choose Vaccine is the earliest upcoming expiry**; the faded render is treated as an export artefact. | The only date that matters to the parent is when the next booster is due. | 35 | proposed |
| D-CHP-09 | **Full warning copy**: "Reservations will stay pending until your pet's required vaccines are uploaded and verified by the front desk." Shown on the vaccines step, on Skip, on the profile and on the home banner. | Completes the clipped Figma string; same rule for Skip. | 36 | proposed |
| D-CHP-10 | **Empty home hides the bookings list but keeps the section header with an empty state**; "View past bookings" is a text link; the Home tab is active on Home. | One consistent empty state instead of two variants; link matches the Figma design. | 16, 17 | proposed |
| D-CHP-11 | **Services row = three tiles (Hotel, Grooming & Spa, Daycare) in one row; no In Home tile.** In Home stays inquiry-only through chat. | D-003 / D-004 / D-011 naming; a fourth tile would only open a chat prompt. | 18, 20, 21 | proposed |
| D-CHP-12 | **Booking cards scroll horizontally** (carousel, snap), stay variant with check-in / check-out and appointment variant with one slot; titles: "Hotel Penthouse / Suite", the package name for grooming, "Daycare full / half day". | Resolves the Spa Grooming / Hotel Spa / Spa naming and the two-up layout. | 18, 19 | proposed |
| D-CHP-13 | **The bell badge counts unread notifications**; the paw tab badge (foundation PhoneShell) should be removed so the count appears once. | The Figma red "1" is ambiguous; one meaning avoids confusion. | 22 | proposed |
| D-CHP-14 | **Notification kinds**: pet_added, pet_approved, vaccine_verified, vaccine_expiring, vaccine_submitted (staff), booking_confirmed, booking_cancelled, payment, appointment_reminder, message. Order / delivery kinds dropped. Relative times with correct grammar, then the date. | UI-kit leftovers removed; grammar fixed. | 6, 64 | proposed |
| D-CHP-15 | **One display date format for the customer app: `16 Nov 2026`, time `10:00 AM`.** | Ends the six formats seen in the export. | 3 | proposed |
| D-CHP-16 | **Emergency contact per pet** (name, phone, relationship) in a new `emergency_contacts` table, optional, with a household fallback. | Not in Figma; the desk needs someone to call. | - | **needs Justin** (scope) |
| D-CHP-17 | **Removing a pet is a soft delete** (`pets.status = inactive`) after confirmation and is blocked while the pet has upcoming bookings; the front desk can restore. | Keeps vaccine and booking history; avoids orphaned bookings. | 30 | proposed |
| D-CHP-18 | **Customers pick a preferred Petrock on the home**; it sets the session location and `customers.home_location_id`, and booking flows start there. | Two locations must always be explicit (D-019). | - | proposed |
| D-CHP-19 | **New pets start `pending`; skipping vaccines marks `needs_details`; only staff set `approved`.** Uploading a proof moves needs_details back to pending. | Matches R-A05 and the front desk verification queue. | 34 | proposed |
| D-CHP-20 | **Breeds and colours are an extendable lookup table (`pet_lookups`)** with inline "+ Add"; vets likewise via the `vets` table. | Entities doc: lookup lists extendable inline via "+". | - | proposed |
