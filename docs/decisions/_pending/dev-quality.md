# Pending decisions - dev-quality

Proposed by the dev-quality module agent on 2026-09-18. The integrator assigns D-0xx ids when merging into `docs/decisions.md`.

| id (placeholder) | decision | rationale | source question | status |
| --- | --- | --- | --- | --- |
| DQ-1 | Dev-quality rules use the id band **R-X80..R-X89**; other modules should take R-X04.. upward | Parallel module agents add owner rules as R-Xnn; a reserved high band avoids id collisions at merge time | CLAUDE.md "new owner rules are R-Xnn" | proposed |
| DQ-2 | **Performance budgets are rows** in `perf_budgets` (js_total 500 kB gzip, largest chunk 450 kB, css 60 kB, docs 1.2 MB, localStorage 3.5 MB, seed 400 ms, first render 1.5 s, 220 routes, 120 assets), warn at 80 % | Same principle as pricing: limits in tables, pages compute. The app ships as one chunk today (415 kB gzip) so the chunk budget equals the total; code-splitting per surface is the lever when it trips | build-plan phase 3 | proposed |
| DQ-3 | **Historical seed** covers check-outs from 365 to 22 days ago with `quote: null` on bookings (invoice keeps the lines); own PRNG stream (seed 20250918) | Reports need a year; keeping quotes off historical rows holds the localStorage snapshot at ~1.5 MB of the ~5 MB origin cap; a separate stream keeps core's "today" data stable | dev-quality scope | proposed |
| DQ-4 | **Layout adoption is opt-in**: pages call `useLayout(spec)` from `src/layout/useLayout.ts`; the spec stays the source of section names and new sections append after saved ones | No shared-file edit needed; foundation pages adopt when they are next touched | hoy pattern | proposed |
| DQ-5 | **A11y checks are heuristics with exemptions** (visually-hidden native inputs, checkbox / radio / range / file, inline links, controls in tables; `target-size` and `heading-skip` are warnings) | Keeps the QA script's exit code about layout and crashes, while still surfacing WCAG 2.5.8 / 1.3.1 gaps | WCAG 2.2 AA | proposed |
| DQ-6 | **QA scripts spawn their own preview server and refuse a busy port** (`--port`, `QA_PORT`, `QA_NO_SERVER=1` to reuse one) | Several agents run previews on one machine; the first run silently captured another worktree's build on a shared port | incident during this build | proposed |
| DQ-7 | The docs module got a **two-line edit** (sidebar title filter + link to D-18) and the D-04 table manager a **one-line link** to the workbench | Discoverability; both are foundation modules, not parallel module agents' files, so merge risk is nil; flagged in notes_for_merge | CLAUDE.md "never edit another module's files to add a page" | needs integrator ack |
| DQ-8 | **CSV imports of more than one row need a manager PIN**; a single-row import behaves like Add row | Bulk changes are as risky as deletes (R-P01); one row is an ordinary edit | R-P01 | needs Justin (policy) |
