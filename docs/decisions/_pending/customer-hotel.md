# Pending decisions - customer-hotel (C-30..C-41)

Choices the module builder made where the designs conflict or are silent (`docs/figma/open-questions.md`). The integrator appends the accepted rows to `docs/decisions.md` with real D-numbers; rows marked **needs Justin** affect pricing or scope and are provisional until he confirms.

| id | decision | rationale | source question |
| --- | --- | --- | --- |
| D-CH-01 | **Step order**: pets & dates (C-30) → room type (C-31) → pet stay details (C-32) → grooming (C-33) → customer details (C-34) → estimate (C-35) → payment (C-36) → confirmation (C-37). | Pets first so the room step can apply the 55 lb / 30 lb fit rules and count rooms; estimate right before payment because its two buttons (deposit / full) are the payment paths in the Figma Estimate. | Q38 (build-blocking) |
| D-CH-02 | Room-share question is a **Yes / No select**, shown only when 2+ pets are picked; more than `settings.hotel_booking.max_pets_per_room` (3) pets forces separate rooms. Sharing = one room with N dogs (multi-dog discount); not sharing = one room per pet. | Choose Pets-3 resolved the placeholder text input into a dropdown; pricing per room follows the discounts table (R-E01..E03). | Q39 |
| D-CH-03 | The **inline calendar drives the check-in / check-out fields** (tap check-in day, then check-out day); the date buttons only switch which end is being picked. Times are separate pickers bounded by the location's opening hours for that weekday; closed days are disabled. | One source of truth for the range; the Figma calendar had a 31-day February and unrelated field values. | Q40 |
| D-CH-04 | `Hotel Reservation.png` is treated as **deprecated**; the step is titled "Choose pets & dates". | Same step minus the grooming question; the later Choose Pets frames are the intended design. | Q41 |
| D-CH-05 | Pet stay details form is **per pet** (one collapsible section each, prefilled from the pet profile); single title; the unlabeled date is **last flea dose**. | Three title variants collapse into one; the medical questionnaire belongs to the pet on this stay (booking_pets + booking_pet_care). | Q42 |
| D-CH-06 | **One Customer details form** serves contact and billing (no separate Billing Details step); labels Mobile / Alt phone. | Duplicate US-address forms add friction; the card form carries its own billing ZIP. | Q44 |
| D-CH-07 | **Estimate maths** come only from the pricing engine: nightly rate by Mon-Thu / Fri-Sun and season, multi-dog discount per dog per night, long-stay % only when paid in full and no holiday nights, tax at the boarding rate, then the card fee on the amount charged now. Nights = calendar days between check-in and check-out (min 1). Room card price = average nightly rate for the chosen dates per pet. | Figma numbers were inconsistent placeholders ($15.50 × 2 = $31 vs $200 / $232.09 / $232.90). | Q45, Q48 |
| D-CH-08 | The estimate **merges both Figma variants**: Payment Details card and the card-fee footnote, plus the "pay in full to get the discount" hint only when a long-stay discount is on the table. | Both pieces of information matter to the pet parent. | Q46 |
| D-CH-09 | **Deposit = 30 % of the pre-fee total** (`settings.hotel_booking.deposit_percent`), balance at check-in. Card fee (3.89 %, fees table) applies to whatever is charged by card now (deposit or full). Free-cancellation window 48 h (setting). **needs Justin** | No deposit amount in any design; percentage lives in settings so the owner can change it. | Q47 |
| D-CH-10 | **Grooming during a stay**: one Grooming & Spa package per pet (priced by the pet's size) plus add-ons, quoted with the service tax, and created as `appointments` rows linked by `booking_id`, starting 09:00 on the check-out day, status requested. | Figma copy: grooms are done at the end of hotel stays; extra grooms via the front desk. | Q49 |
| D-CH-11 | **Payment methods**: credit card (Stripe-shaped form through the PaymentProvider; mock today) or cash at location (nothing charged, booking `payment_status` pending, desk collects). No Apple Pay / PayPal / saved cards yet. | Matches Payment-1 / Frame 1171276427; Stripe is on the kanban. | Q55 |
| D-CH-12 | **Initial status**: an app booking is `requested`; when any pet has a required vaccine that is not verified it is `pending_vaccines`. The front desk confirms (→ `confirmed` = "Upcoming"). Customer labels from `BOOKING_STATUS_CUSTOMER_LABEL`. | One lifecycle (D-019); "Pending Verification" in the Home design maps to pending_vaccines. | Q70 |
| D-CH-13 | **Change / cancel**: customers file `booking_change_requests` (modify dates, add / remove pet, add grooming, cancel, other). Requested / pending stays cancel immediately (refund of the deposit recorded as a status change); confirmed stays create an open cancel request the desk approves with a manager PIN; checked-in stays cannot be changed in the app. | No Figma screen; D-003 asks for consistent hotel reservations across surfaces, and the desk already has PIN-gated cancellation. | Q72, Q104 |
| D-CH-14 | **Room fit rules both apply**: >= 55 lb → Suite only (R-X01, blocks the Penthouse card); >= 30 lb in a Penthouse needs one of the 6 bottom rooms (R-E09), the app counts bottom rooms free for the dates and blocks when none are left. Weight in lbs. **needs Justin** (55 lb rule is still `requested` in the registry) | Both rules are registered; blocking in the app avoids a booking the desk cannot honour. | D-006, R-E09, Q4 |
| D-CH-15 | **Capacity check** in the app: `capacities.max_simultaneous` per room type per location minus overlapping active stays (requested, pending_vaccines, confirmed, checked_in); a stay with pets not sharing counts one room per pet. | R-E11 / R-E12 values are per location; prevents over-booking from the app. | Q60 (partly) |
| D-CH-16 | **Suite description** gets its own copy (premium bed, toys, potty pads, playtime, 2 walks per day, bedtime tuck-in). **needs Justin** | Figma repeats the Penthouse text on the Suite card. | Q48 |
| D-CH-17 | **My reservations** lists hotel stays, grooming appointments and daycare days together (Upcoming / Previous, kind filter); only stays link to a detail page until the grooming / daycare modules register theirs. | The Bookings tab in the Figma tab bar is one list; keeps one place for everything. | Q18 |
| D-CH-18 | The invoice page (C-40) renders the issued invoice, or the booking estimate as a draft when no invoice exists (cash bookings are invoiced at the desk); footer from `settings.invoice`. | R-H09 / R-H10; keeps the customer view honest before the desk invoices. | Q74 |
