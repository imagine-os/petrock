# Pending decisions - extras-manual-website

Choices the module made where the designs or the brief left a gap. `id` is a placeholder until the integrator appends them to `docs/decisions.md`. "needs Justin" marks pricing- or scope-affecting choices.

| id | decision | rationale | source question |
| --- | --- | --- | --- |
| D-P01 | The rebuilt website lives at `/#/site/home`, `/#/site/hotel`, … (P-01..P-12) beside the P-00 placeholder at `/#/site`; the integrator repoints `/site` to P-01. | The public module owns `/site`; registering the same path from two modules would warn and depend on module name order. | build plan: website rebuild P-01..P-19 |
| D-P02 | Website prices are teasers ("from …") computed from the pricing tables through the same helpers as the app; the binding quote is in the app (R-X41). | Never hardcode a price; the site must not disagree with the estimate. | project brief pricing rule |
| D-P03 | Only reviews with `status = published` appear on the website; F-65 Approve = published, Archive = archived (R-X40, R-M12). Two sentiment tags per review are kept as free `tags`. | Open question 111 (does Approve = Publish?). | open-questions.md 111 |
| D-P04 | Website contact form writes `site_inquiries` at the visitor's chosen location; the desk handles them in F-61 with statuses new / seen / replied / closed; reply target one business day (R-X42, requested). **needs Justin** | No inquiry channel existed in the designs; the desk already owns messages. | project brief (website rebuild) |
| D-P05 | FAQs are a table (`site_faqs`, topic, sort_order, published) edited in the table manager, not code. | Owners change copy without a deploy. | - |
| D-P06 | Ops manual chapters are markdown with front matter (title, code, roles, part, version, updated, summary, rules) under `docs/ops-manual/en/`; each chapter is its own route and page code (M-10..M-27); M-01 cover, M-02 fallback, M-03 pending. Parts I–VI as listed in `docs/ops-manual/README.md`. | hoy pattern (docs/reference/hoy-patterns.md section 6) adapted to one code per chapter so specs, screenshots and page docs exist per chapter. | project brief (business operations manual) |
| D-P07 | Live data in chapters comes only from `LiveBlock` directives; a typed number in a chapter is a bug (R-X43). | Manual must stay true when settings change. | hoy rule |
| D-P08 | Each chapter has an in-person and an online lesson; staff record both, managers sign off in-person in F-66; stored in `training_completions` per employee per chapter per mode (R-X44). | The brief asks for in-person and online lessons per role. | project brief |
| D-P09 | Manual audience labels: all staff, front desk, groomer, manager, owner; role -> audience mapping in `manualIndex.ts` (manager also reads front desk chapters, owner also reads manager chapters, super admin reads all). Manual routes are open to all staff roles; customers do not see the manual. | The stub gave ALL_SIGNED_IN; a staff operations manual is not customer-facing. | stub M-01 |
| D-P10 | Reports: periods 7 / 30 / 90 days ending today; collected = payments with status paid by paid_at; service from invoices.source_type; occupancy excludes cancelled and no_show; money needs `reports.financial` (owner), counts are for managers (R-X46). | Reports have no design (empty shell); permissions from R-L05. | D-005, report.jpg |
| D-P11 | Reviews moderation F-65 is the front-desk/manager queue; the admin module's A-35 can reuse `ReviewListItem` and the same table (no second component). | "reviews moderation reuse" in the module scope. | reviews.jpg |
| D-P12 | Walking (F-67) is a real walk log (`walks`: pet, handler, start, minutes, status, potty, note) rather than an empty placeholder; billing the daycare "Walk" item from walks is deferred (R-F05). **needs Justin** (is Walk $12 a daycare add-on or a separate service?) | Open question 61. | open-questions.md 61 |
| D-P13 | Management (F-68) = tasks and checklists per location (`tasks.kind`); Employees > Check List from the Figma nav is a checklist kind, not a separate page (R-L07). Delete is PIN-gated (R-X47). | Empty shells in the design; R-P01 makes deletes approval-gated. | education-1.jpg, employees-1.jpg |
| D-P14 | Notification kinds are a fixed vocabulary (R-M07): booking_confirmed, booking_requested, payment, vaccine_expiring, vaccine_submitted, pet_added, message, review, feedback, system; UI-kit leftovers (orders, deliveries, vet reminders) are dropped. Staff notifications are per user and marked read on open (R-X48). | Open question 6 (placeholder leftovers). | open-questions.md 6 |
| D-P15 | Voice / video call icons in the chat design are not built (R-M11 requested); images on messages are supported by the schema. | Scope open in the design. | R-M11 |
| D-P16 | Owner rules first registered by this module use ids R-X40..R-X48 to leave R-X03..R-X39 to other modules; the integrator renumbers on collision. | Parallel modules cannot coordinate ids. | CLAUDE.md rules contract |
