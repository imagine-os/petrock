# 0010 - Figma fidelity part (a): tokens, fonts, icons, core components

date: 2026-09-18 · from: Justin Massion (Slack thread 1789669007.022039) via the workflow orchestrator · surface: design system · follows: prompt 0009 (audit)

## Prompt

Justin: "I need you to get the design to actually look closer to the artwork. From figma, the icons, the colors, especially the homepage on mobile, everything needs to match the exact styling from before. Please fix this at the design system level"

Orchestrator task (summary): implement plan part (a) of `docs/design/fidelity-audit.md` on main: (1) `tokens.ts` to the Figma palette / type scale / radii / spacing / shadows, keep semantic names and the light / dark x petrock / sunset structure, regenerate `tokens.css`, load the correct font family (verify in Playwright; @fontsource preferred, Google Fonts link fallback, metric fallback last); (2) import the Figma SVGs as icon components behind `<Icon name>` and replace the generic glyphs in BottomNav, headers, services tiles, settings rows, sidebar, top bar and status chips, full gallery in the Icon meta; (3) restyle the shared components per the audit with tokens only; (4) `npm run build` green, screenshots of C-10, C-13, C-30, C-70, F-01, F-10, HUB-01 at 390 / 1280 light + dark, `npm run qa:responsive` at 0 failing; commit in logical chunks and push. Figma API not to be called (quota until ~Sep 22).

## Response

Shipped as `feat(design): ...` commits on main: `e42049e` (tokens + fonts, D-185..D-188), `b808cd4` (icons), `739be3e` (components + shells), then `docs(design)` (this log, changelog draft, kanban, screenshots, QA report).

**Fonts.** `@fontsource/open-sans@5.3.0` (400 / 600 / 700) and `@fontsource/be-vietnam-pro@5.3.0` (400 / 500 / 600) installed through the proxy and imported in `src/main.tsx`; the Google Inter link is gone from `index.html`; `global.css` carries `Open Sans Fallback` / `Be Vietnam Pro Fallback` metric overrides and `h1..h4` use `--lh-title` 1.35 with no negative tracking. Playwright check on `/#/app`: `document.fonts` reports Open Sans 400 / 600 / 700 loaded, body and titles computed as `"Open Sans"`. D-185 logged (proposed, implemented, needs Justin) and the CLAUDE.md theming line updated.

**Tokens.** `src/design/tokens.ts` rewritten from the audit delta table (section 1.8): neutrals extended (`#181818`, `#000000`, `#808080`, `#A1A1A1`, `#DFDFDF`, `#D4D4D4`, `#D0D5DD`, `#F1F1F1`, `#F3F3F3`, `#33363F`, `#304050`, `#3B4256`), statuses `#27B14E` / `#039B00` / `#148F00`, `#FF7A00`, `#FF6868`, `#FD866E`, `#BF0000`, `#25D9AB` / `#E9FBF7`; 30 new semantic roles (three phone tones, band, title, label, secondary / option text, six border roles, tint band, icon purple / header / muted, table head / zebra, completed); `bookingHues` moved to the Figma flat fills (confirmed `#27B14E`, pending `#FF7A00`, checked_out `#25D9AB`); type scale + `--font-display`, `18` / `22`, `--lh-title`, `--ls-body` / `--ls-button`; `--r-input` 4, `--r-cb` 3; `--shadow-desk`, `--shadow-pet`, `--shadow-md` = the Figma pair; layout 71 / 80 / 243 / 48 / 40 / 28 / 44 / 49. Dark theme re-derived (bg `#14131A`, surface `#1C1C24`, text `#F4F6FA`, muted `#A9ABBD`, primary `#B18AE0`, table head `{primary700}`); `sunset` resolves every placeholder. Documented in `docs/design/tokens-draft.md` section 7.

**Icons.** `src/components/atom/Icon/figmaIcons.ts` (generated from the 20 relevant exports by a Python pass over the SVG XML, plus 12 hand-drawn entries) is a second registry `ICON_SVGS` of `{ viewBox, paths[{ d, stroke?, sw?, evenodd?, opacity?, fill?, dash? }] }`; `Icon.tsx` renders either registry behind one `IconName`, default stroke 1.5. Exports: row-chevron, calendar-filled, info-filled, tag, lock-filled, logout-filled, message-filled, moon-filled, question-filled, settings-filled, shield-filled, trash-filled, user-filled (x3), users-filled, vet, clipper, medal, category. Redrawn from the Navbar vector boxes (`I1805:24880;*`, no path data in the node JSON, so geometry from the bounding boxes + `Home Page.png`): nav-home, nav-ticket (dashed perforation), nav-paw (fixed-colour disc `#383430` rings, `#492678` detail, white pads), nav-settings (8-tooth gear); provisional line art from `Services.png`: svc-hotel, svc-spa, svc-daycare, svc-inhome; plus ticket, chat-notification, clock-filled, paw-filled. Mapping tables (`NAV_GLYPH`, `TILE_ICON`, `FILLED_ICON`, `NOTIFICATION_KIND_ICON`) swap the generic names the pages already pass, so the bottom nav, services tiles, settings rows and notification discs show the Figma glyphs without touching module files. Icon meta gallery lists both sets by group.

**Components.** Per audit section 3: BottomNav, CustomerScreenHeader (absorbs PhonePageHeader), PhoneShell (`spec.tone`, mobile field skin), Button, Input / Select / Textarea, Checkbox, Badge / StatusBadge (`variant` fill / pill / text), Avatar (`ring`), IconButton, Card, Section, Tabs (`split`), SegmentedControl, Stepper, Modal (`alert`), StatTile, HomeServiceTile, PetAvatarCard (`filled`, `shadow-pet`), CustomerBookingCard, AccountMenuRow, NotificationRow, DataTable (purple head, 44 px rows, column `tone`), Sidebar (243 borderless, logo, Log Out, codes dev-only), TopBar (80 px: search, location, New booking, chat, Help, bell, user; theme / brand inside the user menu), DesktopShell (`#F4F0FF`, desk skin). Component CSS uses tokens only; the two shells expose `--r-field`, `--color-border-field`, `--fs-field`, `--fw-field`, `--section-title-*`, `--shadow-card`, `--color-border-card-skin` so one stylesheet serves phone and desk.

**Verification.** `npm run build` green; screenshots for C-10, C-13, C-30, C-70, C-80, F-01, F-10, HUB-01 at 390 / 1280 light + dark regenerated (no console errors); `npm run qa:responsive`: 1810 cells (181 routes x 5 widths x 2 themes), 0 failing, a11y findings 8730 -> 8356 (`docs/qa/responsive-report.md`).

**Left for parts (b) / (c)** (see the changelog draft's "For the screen agents"): C-10 layout (HomeHero, 4 tiles on the `--color-band`, pets strip, upcoming cards, "View Past Reservations"), per-screen `spec.tone`, removing the Chat tab from the customer nav, hotel flow without stepper (D-188), front desk pages on the new shell, RoomTimeline hues. The `svc-*` glyphs and the sidebar item icons need the Design System export after the Figma quota resets (~Sep 22).
