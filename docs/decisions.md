# Decision log

One row per decision Justin (or the team) has made. Append-only: never renumber; if a decision is reversed, add a new row that supersedes it and set the old row's status to `superseded by D-0xx`. The `source` column points at the prompt file that carries the verbatim words.

Statuses: `decided` (final unless reversed), `pending` (decided in principle, details still to confirm), `superseded`.

| # | Date | Decision | Source | Status |
|---|---|---|---|---|
| D-001 | 2026-09-17 | **Sections 22 and 14** of the Figma page are the approved design scope (customer mobile app and front-desk web). Older sections and rows are reference only, but must be mined for useful screens that are missing from 22/14 (gap check pending). | prompt 0002, answer 1 | decided |
| D-002 | 2026-09-17 | **Control Panel screens 1-15** are owner / super-admin surfaces, not front desk. Role management will allow adjusting who sees them later, so access must be role-driven rather than hard-coded. | prompt 0002, answer 2 | decided |
| D-003 | 2026-09-17 | **Day care**: not designed yet (verify against the Figma inventory). **In Home**: not built, out of the initial scope. **Vaccines** are a first-class flow throughout the system (customer app and front desk), so a full vaccine flow is expected. **Hotel reservations** must be clear and consistent across all surfaces. | prompt 0002, answer 3 | decided |
| D-004 | 2026-09-17 | **Spa and Grooming are one service.** Build one flow and use one name (naming TBD; proposal: "Grooming & Spa"). The Figma "Spa Flow" and "Grooming" sections are treated as variants of the same flow. | prompt 0002, answer 4 | decided (name pending) |
| D-005 | 2026-09-17 | The old single-screen rows **report, education, reviews, employees, walking, management** go into an **"Extras" category** for now. **Reports / analytics are in scope.** **Employees is required** (it is the user-management surface). Dog walking and management can be extended later if needed. | prompt 0002, answer 5 | decided |
| D-006 | 2026-09-17 | A **business rules registry** is required. Every rule appears (a) on the spec builder / page inspector of the page that uses it and (b) in a central **Settings > Rules** area, with the ability to add new rules and a **status per rule** (requested / in dev / implemented / etc.). Justin will supply more rules later. Seed rules from Figma: "Dog 55lb or greater must be a Suite"; "Daycare shorter than X leaves no time for spa"; pricing rules (TBD). | prompt 0002, answer 6 | decided (rule list pending) |
| D-007 | 2026-09-17 | There is **no dedicated design-system page** in Figma to rely on. **Rebuild the design system fresh** from the main page's icons, fonts and colors, clean and complete. It must support **light / dark mode** and make **new themes easy** to add later. | prompt 0002, answer 7 | decided |
| D-008 | 2026-09-17 | **Timeline view** resolved from the UX-plan sitemap: **Hotel & Daycare reservations get Table + Timeline views; Spa gets Table + Board (kanban)**. The Section 14 timeline frames are relabelled grooming skeletons, so the timeline will be designed fresh around rooms and stays. (Justin's expectation was hotel-only; sitemap adds daycare.) See `docs/figma/deep-dive.md` section 2. | prompt 0002, answer 8; deep-dive | decided |
| D-009 | 2026-09-17 | **Santa-Maria-Tenis-Club/os**: Justin reports GitHub connected to Santa-Maria-Tenis-Club (2026-09-17). Repo still not visible to Claude Tag (checked 18:54 UTC; `create_session` with it as source was denied). Justin reports connected again 19:17 UTC; still not visible to Claude Tag at 19:18 UTC (`create_session` denied). The Claude GitHub App must be installed on the Santa-Maria-Tenis-Club GitHub organization and the repo enabled at https://claude.ai/admin-settings/claude-tag; Company-OS (Playset-LLC) and imagine-os repos are visible, so the gap is that org specifically. Digest pending; it will run in its own session once visible (one-owner-per-session rule). | prompt 0002, closing line; prompt 0003 | blocked |
| D-010 | 2026-09-17 | **Port from older sections** (screens with no counterpart in Sections 22/14, see `docs/figma/deep-dive.md` section 1): Choose Vaccine, Invoice / booking detail, employees list + add-employee popup, front-desk home stats strip, owner KPI dashboard, OTP verify modal, collapsible nav + location switcher, form-field redlines. | deep-dive (gap check per D-001) | proposed (awaiting Justin) |
| D-011 | 2026-09-17 | **Grooming & Spa**: one flow named **Grooming**; **Spa is a package category** within it; the daycare-duration and second-pet rules are **configurable conditions** (registered in the rules registry, D-006). Refines D-004 (which left the name TBD). | deep-dive section 4 | proposed |
| D-012 | 2026-09-17 | **Figma file is on a Starter plan**; the API caps reads on Starter files regardless of the caller's seat (Figma rate-limit docs, tiered limits since Nov 2025). Quota exhausted 2026-09-17 after ~40 reads (429, Retry-After ~4.6 days). **Decision: no paid upgrade**; Justin exports Sections 22 and 14 and the Design System page as PNG 2x plus the .fig local copy into `docs/figma/exports` (or Slack). API second pass after ~2026-09-22 for remaining text. If upgrading later: Professional plan, one Dev seat (~$12/mo annual, $15 monthly; unverified from primary page). | deep-dive coverage caveat; prompt 0003 | decided |
| D-013 | 2026-09-17 | **Proposed build plan and stack**: React + TypeScript, mock data first with an adapter for the Company-OS API later, deployed on GitHub Pages; customer app = the same codebase wrapped with **Capacitor** for iOS/Android (unless Justin prefers separate native apps). Order: **foundation** first (tokens/themes, component library with per-component docs, table library, roles + location context, PIN login/approvals, spec builder + rules registry, testing hub with demo users, docs viewer), then the **hotel journey** end to end (customer + front desk), then grooming, daycare, vaccines, reports, extras. | prompt 0002 response | proposed (awaiting go-ahead) |
| D-014 | 2026-09-17 | **Side menu**: navigation items grouped into **collapsible categories** with a **per-category toggle** plus an **expand-all / collapse-all** control, styled to match the design system. Applies to the individualized per-role side menus (role management). | prompt 0003 | decided |
| D-015 | 2026-09-17 | **Figma Design System page (node 58:118)** is a **reference input, not the source of truth**; the code design system is **rebuilt fresh** (see D-007). Justin: "Not sure if its as good as you making it proper but you can reference it". | prompt 0003 | decided |
| D-016 | 2026-09-17 | **Everything must be responsive**, not limited to the designed sizes (390 mobile, 1440 front desk); **responsiveness is checked on every page as part of the definition of done.** Calendar **kanban and timeline** views may **degrade gracefully on phones** (front desk uses an iMac) but should aim for full responsiveness. | prompt 0004 | decided |
| D-017 | 2026-09-17 | **Figma exports received: Drop 1** (`Petrock Main.zip`, 2026-09-17, `docs/figma/exports/petrock-main/`): **238 files: 124 png, 60 jpg, 29 svg, 25 pdf; no `.fig`; mostly 1x** (mobile 390 wide, front desk 1440 wide; `Message Support*`, `Grooming.png`, `front desk-14/20` at 2x). Catalogued in `docs/figma/screen-catalog.md`: **109 distinct screens** (customer mobile 54, front desk 20, owner/admin 24, shared/reference 11) from 238 files; 3 flagged for the second pass. Findings: the `Frame 11712764xx` two-ups are **two light variants, not light/dark pairs** (no dark screen exists in the drop); the 15 numbered PDFs are the **owner Control Panel (Draft)** settings screens; 5 legacy PetLinx screenshots and the Spa 12.7 price card are reference. Derived docs: `docs/data/entities-from-designs.md`, `docs/design/components-from-designs.md`, `docs/rules/business-rules-from-designs.md`, `docs/figma/open-questions.md`. **Screen catalog written.** **Design System page export (6223 items) pending**; `.fig` local copy pending; node-id mapping pending (second Figma pass). | prompt 0004; `docs/figma/screen-catalog.md`; changelog 0006 | in progress (drop 2 pending) |
| D-018 | 2026-09-17 | **Messaging and auth flows may be improved beyond the hand-designed Figma screens using current best practices** (Justin, 2026-09-17 19:33 UTC). Applies to the customer chat / notification centre (`Message Support*`, `notification*`), the front desk inbox (`message*.jpg`) and the sign-up / sign-in / forgot-password screens (`Frame 1171276420-1171276423`); the Figma versions remain reference input, not the spec. | prompt 0004 follow-up (Slack 19:33 UTC) | decided |
| D-019 | 2026-09-18 | **Foundation decisions (prompt 0005 "Please build the whole thing").** Stack per D-013 (Vite + React 18 + TS strict, HashRouter, CSS tokens, mock DataProvider with a Company-OS REST adapter seam, GitHub Pages; Capacitor later). **One booking lifecycle** `requested -> pending_vaccines -> confirmed -> checked_in -> checked_out` (+ `cancelled`, `no_show`) on every surface. **Roles** `super_admin, owner, manager, front_desk, groomer, customer, public`; string permissions; super admin "view as". **Location** always known: front desk / groomer / manager pinned, owner / super admin switch or see all. **PIN**: 4-6 digits; manager PIN gates cancel / no-show / reopen / confirm-without-vaccines, refunds, discounts, deletes; approvals audited. Where designs conflict the build chose: daycare full-day threshold **6 h**, card fee **3.89 %**, tax **2 %**, Platinum prices from the Spa card, Diamond prices placeholders, weight in **lbs** with size bands S<20 M<40 L<70 XL<100 Giant. Second brand `sunset` exists only to prove theming. | prompt 0005; changelog 0007; `CLAUDE.md`; `docs/build-plan.md` | decided (thresholds pending Justin) |
| D-020 | 2026-09-18 | **customer-auth**: Welcome screen uses the right-hand Figma variant: logo, "New to Petrock?", full-width **Create account**, "Already have an account? Sign in". Headline is the company tagline; a service-chip row and a locations line were added. _Rationale: The left variant has no CTA; D-018 allows improving auth beyond Figma._ | Q12 (Frame 1171276420) (build decision, was D-pending-auth-1 in the module draft) | decided |
| D-021 | 2026-09-18 | **customer-auth**: Sign in and Forgot password get their own copy ("Welcome back", "Forgot your password?"); the "Create your Account / New To Petrock?" header stays only on sign-up. "Forget password" -> "Forgot password". _Rationale: Copy errors flagged in the catalog (R-M15)._ | Q13 (Frame 1171276422/423) (build decision, was D-pending-auth-2 in the module draft) | decided |
| D-022 | 2026-09-18 | **customer-auth**: **Remember me** kept as a checkbox ("Remember me for 30 days", default on) stored on `auth_credentials.remember_me`; enforcement (session expiry when off) needs a hook in `SessionProvider` (foundation) - see gaps. _Rationale: Design shows the toggle; best practice is explicit duration._ | Q14 (build decision, was D-pending-auth-3 in the module draft) | decided |
| D-023 | 2026-09-18 | **customer-auth**: **OTP step is built** (D-010): 6-digit code, 10 min expiry, 5 attempts, resend after 30 s, delivered by email (SMS later). Used for email verification after sign-up, on first sign-in when the email is unverified (modal), and for password reset. No OTP on every sign-in. _Rationale: Keeps sign-in one step; reset and verification are where codes add security._ | Q14, D-010 (build decision, was D-pending-auth-4 in the module draft) | decided |
| D-024 | 2026-09-18 | **customer-auth**: **Password policy**: min 8 characters, at least one letter and one number, not in a small common-password list; strength meter; no forced symbols, no expiry. Placeholder "#123@156" dropped. **needs Justin** (policy is a `settings.auth.policy` value). _Rationale: NIST 800-63B; Figma placeholder implied symbols._ | Q14 (R-M14) (build decision, was D-pending-auth-5 in the module draft) | superseded by D-184 (mock values only) |
| D-025 | 2026-09-18 | **customer-auth**: **Lockout**: 5 failed sign-ins lock the account 15 minutes (C-07 with countdown); reset password clears the lock. Values in `settings.auth.policy`. **needs Justin** _Rationale: Best practice against credential stuffing._ | new (D-018) (build decision, was D-pending-auth-6 in the module draft) | superseded by D-184 (mock values only) |
| D-026 | 2026-09-18 | **customer-auth**: Forgot password never reveals whether an email exists; the mock shows a demo hint only. _Rationale: Account-enumeration protection._ | new (D-018) (build decision, was D-pending-auth-7 in the module draft) | decided |
| D-027 | 2026-09-18 | **customer-auth**: Sign-up asks for the **usual Petrock location** (Encino / Westwood) -> `customers.home_location_id`, and an optional mobile number; email is the login id. _Rationale: Location is a first-class concept (D-019); phone needed for pickup texts._ | entities 1 / 20 (build decision, was D-pending-auth-8 in the module draft) | decided |
| D-028 | 2026-09-18 | **customer-auth**: Terms + Privacy acceptance is a required checkbox (`auth_credentials.terms_accepted_at`); marketing opt-in is separate and optional (`customers.marketing_opt_in`). Legal pages link to the public site until the settings module ships them (C-7x). _Rationale: App-store requirements._ | Q9 (marketing opt-in dropped in redesign) (build decision, was D-pending-auth-9 in the module draft) | decided |
| D-029 | 2026-09-18 | **customer-auth**: **Change email / change password / delete account** are not in this module; they belong to customer settings (C-70..C-79). Delete must re-authenticate and create a request (rule R-X39 requested). _Rationale: Scope split per build plan._ | Q15 (build decision, was D-pending-auth-10 in the module draft) | decided |
| D-030 | 2026-09-18 | **customer-auth**: Emails can sign in **before** verification and browse; booking / payment flows must check `auth_credentials.email_verified` and send to C-04 (rule R-X33, in_dev for the hotel / grooming / daycare modules). _Rationale: Lower friction; verification still gates money._ | new (D-018) (build decision, was D-pending-auth-11 in the module draft) | decided |
| D-031 | 2026-09-18 | **customer-auth**: Onboarding popup = three-slide **OnboardingIntroModal** (add dogs, upload vaccines, book) shown once on the welcome screen (700 ms after load, remembered in localStorage) and re-openable from C-01 and C-08. _Rationale: Scope item "onboarding popup"; nothing designed._ | new (build decision, was D-pending-auth-12 in the module draft) | decided |
| D-032 | 2026-09-18 | **customer-home-pets**: **Add pet wizard = 5 steps in one stepper**: Basics, Size (lbs), Personality & feeding, Vet & emergency, Vaccines. Socialisation / personality / treats live only on step 3. _Rationale: Figma had a 2-step stepper but three screens and duplicated questions; five short steps keep each screen one scroll on a phone._ | 23 (build decision, was D-CHP-01 in the module draft) | proposed |
| D-033 | 2026-09-18 | **customer-home-pets**: **Personality is a radio group; meals per day is slot based** (AM, PM, AM & PM, AM / Mid Day / PM, Free feeding), and feeding instruction fields show only for the chosen slots. _Rationale: Radio matches the Figma design; slots (not a count) drive the per-slot instructions the desk needs._ | 24 (build decision, was D-CHP-02 in the module draft) | proposed |
| D-034 | 2026-09-18 | **customer-home-pets**: **Weight in lbs everywhere**, numeric input with an lbs suffix; size band S <20, M <40, L <70, XL <100, Giant 100+ computed and stored on save. _Rationale: Room fit and grooming tiers are in lbs (D-019); the Kg profile is a UI-kit screen._ | 4, 28 (build decision, was D-CHP-03 in the module draft) | proposed (bands **need Justin**, per D-019) |
| D-035 | 2026-09-18 | **customer-home-pets**: **Type defaults to Dog; Cat / Other stay selectable but all copy says dog.** _Rationale: The hotel is dog-only in practice; the field stays open so no data model change is needed later._ | 25 (build decision, was D-CHP-04 in the module draft) | proposed (needs Justin) |
| D-036 | 2026-09-18 | **customer-home-pets**: **My Pets grid and a pet-only Pet profile are in scope; the UI-kit Reminder / Notes list and owner e-mail / phone on the pet form are not.** _Rationale: Reminders and notes duplicate notifications and bookings; owner fields belong to the account._ | 26, 27 (build decision, was D-CHP-05 in the module draft) | proposed |
| D-037 | 2026-09-18 | **customer-home-pets**: **One proof per vaccine record** (per-vaccine uploads), accepted .jpg .png .pdf; .gif not accepted until confirmed. Proof is required to submit a record. _Rationale: Staff verify vaccine by vaccine; a bulk upload cannot be matched to a type._ | 32, 33 (build decision, was D-CHP-06 in the module draft) | proposed (needs Justin) |
| D-038 | 2026-09-18 | **customer-home-pets**: **Vaccine status per record derives from the record and today**: missing / pending (submitted) / verified / expired (expires_on before today even if verified) / rejected. Pet-level approval: Approved (staff) · Pending verification · Needs more details (a required vaccine missing or skipped) · Vaccine expired. _Rationale: Answers which states exist and who sets them: customers upload, the front desk verifies and approves._ | 34 (build decision, was D-CHP-07 in the module draft) | proposed |
| D-039 | 2026-09-18 | **customer-home-pets**: **The pet-card date on Choose Vaccine is the earliest upcoming expiry**; the faded render is treated as an export artefact. _Rationale: The only date that matters to the parent is when the next booster is due._ | 35 (build decision, was D-CHP-08 in the module draft) | proposed |
| D-040 | 2026-09-18 | **customer-home-pets**: **Full warning copy**: "Reservations will stay pending until your pet's required vaccines are uploaded and verified by the front desk." Shown on the vaccines step, on Skip, on the profile and on the home banner. _Rationale: Completes the clipped Figma string; same rule for Skip._ | 36 (build decision, was D-CHP-09 in the module draft) | proposed |
| D-041 | 2026-09-18 | **customer-home-pets**: **Empty home hides the bookings list but keeps the section header with an empty state**; "View past bookings" is a text link; the Home tab is active on Home. _Rationale: One consistent empty state instead of two variants; link matches the Figma design._ | 16, 17 (build decision, was D-CHP-10 in the module draft) | proposed |
| D-042 | 2026-09-18 | **customer-home-pets**: **Services row = three tiles (Hotel, Grooming & Spa, Daycare) in one row; no In Home tile.** In Home stays inquiry-only through chat. _Rationale: D-003 / D-004 / D-011 naming; a fourth tile would only open a chat prompt._ | 18, 20, 21 (build decision, was D-CHP-11 in the module draft) | proposed |
| D-043 | 2026-09-18 | **customer-home-pets**: **Booking cards scroll horizontally** (carousel, snap), stay variant with check-in / check-out and appointment variant with one slot; titles: "Hotel Penthouse / Suite", the package name for grooming, "Daycare full / half day". _Rationale: Resolves the Spa Grooming / Hotel Spa / Spa naming and the two-up layout._ | 18, 19 (build decision, was D-CHP-12 in the module draft) | proposed |
| D-044 | 2026-09-18 | **customer-home-pets**: **The bell badge counts unread notifications**; the paw tab badge (foundation PhoneShell) should be removed so the count appears once. _Rationale: The Figma red "1" is ambiguous; one meaning avoids confusion._ | 22 (build decision, was D-CHP-13 in the module draft) | proposed |
| D-045 | 2026-09-18 | **customer-home-pets**: **Notification kinds**: pet_added, pet_approved, vaccine_verified, vaccine_expiring, vaccine_submitted (staff), booking_confirmed, booking_cancelled, payment, appointment_reminder, message. Order / delivery kinds dropped. Relative times with correct grammar, then the date. _Rationale: UI-kit leftovers removed; grammar fixed._ | 6, 64 (build decision, was D-CHP-14 in the module draft) | proposed |
| D-046 | 2026-09-18 | **customer-home-pets**: **One display date format for the customer app: `16 Nov 2026`, time `10:00 AM`.** _Rationale: Ends the six formats seen in the export._ | 3 (build decision, was D-CHP-15 in the module draft) | proposed |
| D-047 | 2026-09-18 | **customer-home-pets**: **Emergency contact per pet** (name, phone, relationship) in a new `emergency_contacts` table, optional, with a household fallback. _Rationale: Not in Figma; the desk needs someone to call._ | - (build decision, was D-CHP-16 in the module draft) | proposed (needs Justin) |
| D-048 | 2026-09-18 | **customer-home-pets**: **Removing a pet is a soft delete** (`pets.status = inactive`) after confirmation and is blocked while the pet has upcoming bookings; the front desk can restore. _Rationale: Keeps vaccine and booking history; avoids orphaned bookings._ | 30 (build decision, was D-CHP-17 in the module draft) | proposed |
| D-049 | 2026-09-18 | **customer-home-pets**: **Customers pick a preferred Petrock on the home**; it sets the session location and `customers.home_location_id`, and booking flows start there. _Rationale: Two locations must always be explicit (D-019)._ | - (build decision, was D-CHP-18 in the module draft) | proposed |
| D-050 | 2026-09-18 | **customer-home-pets**: **New pets start `pending`; skipping vaccines marks `needs_details`; only staff set `approved`.** Uploading a proof moves needs_details back to pending. _Rationale: Matches R-A05 and the front desk verification queue._ | 34 (build decision, was D-CHP-19 in the module draft) | proposed |
| D-051 | 2026-09-18 | **customer-home-pets**: **Breeds and colours are an extendable lookup table (`pet_lookups`)** with inline "+ Add"; vets likewise via the `vets` table. _Rationale: Entities doc: lookup lists extendable inline via "+"._ | - (build decision, was D-CHP-20 in the module draft) | proposed |
| D-052 | 2026-09-18 | **customer-hotel**: **Step order**: pets & dates (C-30) → room type (C-31) → pet stay details (C-32) → grooming (C-33) → customer details (C-34) → estimate (C-35) → payment (C-36) → confirmation (C-37). _Rationale: Pets first so the room step can apply the 55 lb / 30 lb fit rules and count rooms; estimate right before payment because its two buttons (deposit / full) are the payment paths in the Figma Estimate._ | Q38 (build-blocking) (build decision, was D-CH-01 in the module draft) | decided |
| D-053 | 2026-09-18 | **customer-hotel**: Room-share question is a **Yes / No select**, shown only when 2+ pets are picked; more than `settings.hotel_booking.max_pets_per_room` (3) pets forces separate rooms. Sharing = one room with N dogs (multi-dog discount); not sharing = one room per pet. _Rationale: Choose Pets-3 resolved the placeholder text input into a dropdown; pricing per room follows the discounts table (R-E01..E03)._ | Q39 (build decision, was D-CH-02 in the module draft) | decided |
| D-054 | 2026-09-18 | **customer-hotel**: The **inline calendar drives the check-in / check-out fields** (tap check-in day, then check-out day); the date buttons only switch which end is being picked. Times are separate pickers bounded by the location's opening hours for that weekday; closed days are disabled. _Rationale: One source of truth for the range; the Figma calendar had a 31-day February and unrelated field values._ | Q40 (build decision, was D-CH-03 in the module draft) | decided |
| D-055 | 2026-09-18 | **customer-hotel**: `Hotel Reservation.png` is treated as **deprecated**; the step is titled "Choose pets & dates". _Rationale: Same step minus the grooming question; the later Choose Pets frames are the intended design._ | Q41 (build decision, was D-CH-04 in the module draft) | decided |
| D-056 | 2026-09-18 | **customer-hotel**: Pet stay details form is **per pet** (one collapsible section each, prefilled from the pet profile); single title; the unlabeled date is **last flea dose**. _Rationale: Three title variants collapse into one; the medical questionnaire belongs to the pet on this stay (booking_pets + booking_pet_care)._ | Q42 (build decision, was D-CH-05 in the module draft) | decided |
| D-057 | 2026-09-18 | **customer-hotel**: **One Customer details form** serves contact and billing (no separate Billing Details step); labels Mobile / Alt phone. _Rationale: Duplicate US-address forms add friction; the card form carries its own billing ZIP._ | Q44 (build decision, was D-CH-06 in the module draft) | decided |
| D-058 | 2026-09-18 | **customer-hotel**: **Estimate maths** come only from the pricing engine: nightly rate by Mon-Thu / Fri-Sun and season, multi-dog discount per dog per night, long-stay % only when paid in full and no holiday nights, tax at the boarding rate, then the card fee on the amount charged now. Nights = calendar days between check-in and check-out (min 1). Room card price = average nightly rate for the chosen dates per pet. _Rationale: Figma numbers were inconsistent placeholders ($15.50 × 2 = $31 vs $200 / $232.09 / $232.90)._ | Q45, Q48 (build decision, was D-CH-07 in the module draft) | decided |
| D-059 | 2026-09-18 | **customer-hotel**: The estimate **merges both Figma variants**: Payment Details card and the card-fee footnote, plus the "pay in full to get the discount" hint only when a long-stay discount is on the table. _Rationale: Both pieces of information matter to the pet parent._ | Q46 (build decision, was D-CH-08 in the module draft) | decided |
| D-060 | 2026-09-18 | **customer-hotel**: **Deposit = 30 % of the pre-fee total** (`settings.hotel_booking.deposit_percent`), balance at check-in. Card fee (3.89 %, fees table) applies to whatever is charged by card now (deposit or full). Free-cancellation window 48 h (setting). **needs Justin** _Rationale: No deposit amount in any design; percentage lives in settings so the owner can change it._ | Q47 (build decision, was D-CH-09 in the module draft) | proposed (needs Justin) |
| D-061 | 2026-09-18 | **customer-hotel**: **Grooming during a stay**: one Grooming & Spa package per pet (priced by the pet's size) plus add-ons, quoted with the service tax, and created as `appointments` rows linked by `booking_id`, starting 09:00 on the check-out day, status requested. _Rationale: Figma copy: grooms are done at the end of hotel stays; extra grooms via the front desk._ | Q49 (build decision, was D-CH-10 in the module draft) | decided |
| D-062 | 2026-09-18 | **customer-hotel**: **Payment methods**: credit card (Stripe-shaped form through the PaymentProvider; mock today) or cash at location (nothing charged, booking `payment_status` pending, desk collects). No Apple Pay / PayPal / saved cards yet. _Rationale: Matches Payment-1 / Frame 1171276427; Stripe is on the kanban._ | Q55 (build decision, was D-CH-11 in the module draft) | decided |
| D-063 | 2026-09-18 | **customer-hotel**: **Initial status**: an app booking is `requested`; when any pet has a required vaccine that is not verified it is `pending_vaccines`. The front desk confirms (→ `confirmed` = "Upcoming"). Customer labels from `BOOKING_STATUS_CUSTOMER_LABEL`. _Rationale: One lifecycle (D-019); "Pending Verification" in the Home design maps to pending_vaccines._ | Q70 (build decision, was D-CH-12 in the module draft) | decided |
| D-064 | 2026-09-18 | **customer-hotel**: **Change / cancel**: customers file `booking_change_requests` (modify dates, add / remove pet, add grooming, cancel, other). Requested / pending stays cancel immediately (refund of the deposit recorded as a status change); confirmed stays create an open cancel request the desk approves with a manager PIN; checked-in stays cannot be changed in the app. _Rationale: No Figma screen; D-003 asks for consistent hotel reservations across surfaces, and the desk already has PIN-gated cancellation._ | Q72, Q104 (build decision, was D-CH-13 in the module draft) | decided |
| D-065 | 2026-09-18 | **customer-hotel**: **Room fit rules both apply**: >= 55 lb → Suite only (R-X01, blocks the Penthouse card); >= 30 lb in a Penthouse needs one of the 6 bottom rooms (R-E09), the app counts bottom rooms free for the dates and blocks when none are left. Weight in lbs. **needs Justin** (55 lb rule is still `requested` in the registry) _Rationale: Both rules are registered; blocking in the app avoids a booking the desk cannot honour._ | D-006, R-E09, Q4 (build decision, was D-CH-14 in the module draft) | proposed (needs Justin) |
| D-066 | 2026-09-18 | **customer-hotel**: **Capacity check** in the app: `capacities.max_simultaneous` per room type per location minus overlapping active stays (requested, pending_vaccines, confirmed, checked_in); a stay with pets not sharing counts one room per pet. _Rationale: R-E11 / R-E12 values are per location; prevents over-booking from the app._ | Q60 (partly) (build decision, was D-CH-15 in the module draft) | decided |
| D-067 | 2026-09-18 | **customer-hotel**: **Suite description** gets its own copy (premium bed, toys, potty pads, playtime, 2 walks per day, bedtime tuck-in). **needs Justin** _Rationale: Figma repeats the Penthouse text on the Suite card._ | Q48 (build decision, was D-CH-16 in the module draft) | superseded by D-185 |
| D-068 | 2026-09-18 | **customer-hotel**: **My reservations** lists hotel stays, grooming appointments and daycare days together (Upcoming / Previous, kind filter); only stays link to a detail page until the grooming / daycare modules register theirs. _Rationale: The Bookings tab in the Figma tab bar is one list; keeps one place for everything._ | Q18 (build decision, was D-CH-17 in the module draft) | decided |
| D-069 | 2026-09-18 | **customer-hotel**: The invoice page (C-40) renders the issued invoice, or the booking estimate as a draft when no invoice exists (cash bookings are invoiced at the desk); footer from `settings.invoice`. _Rationale: R-H09 / R-H10; keeps the customer view honest before the desk invoices._ | Q74 (build decision, was D-CH-18 in the module draft) | decided |
| D-070 | 2026-09-18 | **customer-grooming-daycare**: A Grooming & Spa order can hold several pets, each with its own package and add-ons, at one time slot with one payment (`grooming_orders` groups the per-pet `appointments`). Pets are groomed side by side up to the location's grooming capacity, so the order's duration is the longest pet time, not the sum. _Rationale: Figma "Groom another pet" and "Re-Create Spa" (Frame 1171276429/34); the front desk still sees one appointment per pet._ | open questions 52, 53 (build decision, was D-0xx-a in the module draft) | decided |
| D-071 | 2026-09-18 | **customer-grooming-daycare**: No slot screen existed in Figma; start times are computed: 30-minute steps inside location hours, the order must end before closing, and booked appointments + pets in the order must stay within `capacities.grooming` (2) for every 15-minute step. A named groomer must also be free. _Rationale: Keeps app requests feasible for the desk; matches R-E10 / R-G17._ | open question 54 (build decision, was D-0xx-b in the module draft) | decided |
| D-072 | 2026-09-18 | **customer-grooming-daycare**: Customers may cancel in the app only while the order / day is `requested` or `pending_vaccines`. Confirmed bookings show "Message the front desk"; staff cancel through the PIN-gated transition (R-I06). Refunds are a desk action. _Rationale: Consistent with the PIN rule; avoids unrefunded card payments disappearing from the desk's view._ | open question 104 (build decision, was D-0xx-c in the module draft) | decided |
| D-073 | 2026-09-18 | **customer-grooming-daycare**: Status after checkout: any pet without verified, unexpired required vaccines -> `pending_vaccines`; card paid -> `confirmed`; pay at location -> `requested`. `appointments.status` (core enum without pending_vaccines) gets `confirmed` or `requested`; the order row carries the lifecycle status. **needs integration** (align the appointments enum). _Rationale: R-A05 and the one-lifecycle rule._ | R-A05 (build decision, was D-0xx-d in the module draft) | decided |
| D-074 | 2026-09-18 | **customer-grooming-daycare**: **needs Justin** - Daycare vs grooming rule (R-X02) implemented as: a day fits a groom when `hours * 60 >= shortest package minutes for the pets' sizes + buffer`, buffer = `settings.daycare.grooming_buffer_min` (default 60). Shorter days show why grooming cannot be added; longer days offer "Add a groom during daycare" and the confirmation links into the grooming flow with the same pets and date. _Rationale: D-011 says the condition is configurable; 60 minutes covers handover both ways._ | open questions 58, 60; D-006 (build decision, was D-0xx-e in the module draft) | proposed (needs Justin) |
| D-075 | 2026-09-18 | **customer-grooming-daycare**: **needs Justin** - Daycare threshold uses `daycare_pricing.threshold_hours` = 6 (Full Day at or above, Half Day below); the Figma "5 h" and the "$45 for Half Day" line are treated as errors and the price is always computed from the two times. Play Hour applies at 1 h or less. Check-out must be at least 1 h after check-in. _Rationale: D-019 chose 6 h pending confirmation; computing removes the mock bug (R-F06)._ | open questions 58, 59 (build decision, was D-0xx-f in the module draft) | proposed (needs Justin) |
| D-076 | 2026-09-18 | **customer-grooming-daycare**: **needs Justin** - Diamond Groom prices shown to customers are the placeholder seed values with a visible "prices pending confirmation" note on the card (R-G06). _Rationale: Better than hiding the tier; the note stops surprises at the desk._ | open question 98, 112 (build decision, was D-0xx-g in the module draft) | proposed (needs Justin) |
| D-077 | 2026-09-18 | **customer-grooming-daycare**: Card payments in the app carry the card fee (3.89 %) as a separate quote line; pay at location carries none and the booking stays `requested` until the desk confirms. Card fields are mock today; `ServicePayMethod` renders a Stripe Elements mount when the provider `needsCardElement`. _Rationale: R-H03; keeps the Stripe swap a provider change._ | R-H03 (build decision, was D-0xx-h in the module draft) | decided |
| D-078 | 2026-09-18 | **customer-grooming-daycare**: Daycare Checkout (empty in Figma) mirrors the grooming checkout: summary, pets, payment method, engine totals. The per-pet questionnaire is stored per booking in `daycare_booking_pets` (flea medication brand / date, medical alert) rather than overwriting the pet profile. _Rationale: Answers can differ per day; the desk reads them on the day view._ | open question 59 (build decision, was D-0xx-i in the module draft) | decided |
| D-079 | 2026-09-18 | **customer-grooming-daycare**: Grooming & Spa and Daycare are not bottom-nav tabs; they are reached from the Home service tiles (`/app/grooming`, `/app/daycare`) and from booking cards. _Rationale: Figma tab bar has four items (Home, Bookings, Paw, Settings)._ | screen catalog 1.2 (build decision, was D-0xx-j in the module draft) | decided |
| D-080 | 2026-09-18 | **customer-grooming-daycare**: Size tiers on customer cards use `pets.size` or the weight bands from `src/domain/booking.ts` (S < 20 lb, M < 40, L < 70, XL < 100, Giant 100+). **needs Justin** to confirm the bands. _Rationale: Working assumption carried from the foundation._ | open question 28 (build decision, was D-0xx-k in the module draft) | proposed (needs Justin) |
| D-081 | 2026-09-18 | **customer-settings-chat**: Chat bubbles follow the platform convention: the customer's own messages on the right in primary purple, Front Desk on the left in a neutral bubble with avatar and name (rule R-M25). _Rationale: Every mainstream messenger does this; the Figma draft inverts it and D-018 allows a best-practice redesign._ | open-questions 66 (build decision, was D-CSC-1 in the module draft) | proposed |
| D-082 | 2026-09-18 | **customer-settings-chat**: One Front Desk thread **per customer per location** (Encino, Westwood); the inbox lists both and offers "Message Westwood" to start a thread; a "Session start" marker opens each thread (R-M20). _Rationale: Core already scopes conversations by location; staff at each desk only see their own threads._ | open-questions 64, 67 (build decision, was D-CSC-2 in the module draft) | proposed |
| D-083 | 2026-09-18 | **customer-settings-chat**: System notifications stay in the notification centre; they are **not** injected into the chat stream (Message Support-2 variant rejected). The centre has two tabs: Notifications and Inbox. _Rationale: Mixed streams make read-state and search ambiguous; the two-tab centre covers both Figma variants._ | open-questions 64, 65 (build decision, was D-CSC-3 in the module draft) | proposed |
| D-084 | 2026-09-18 | **customer-settings-chat**: Chat is inside the tabbed shell (bottom nav visible), full-height with an internal scroll and a sticky composer. _Rationale: Matches Message Support-1 and keeps navigation one tap away._ | open-questions 66 (build decision, was D-CSC-4 in the module draft) | proposed |
| D-085 | 2026-09-18 | **customer-settings-chat**: Languages offered: **English and Spanish** only (R-M26). The five Figma languages (English, Deutsch, French, Dutch, Bulgarian) look like a UI-kit leftover. _Rationale: LA customer base; i18n layer supports en/es today._ | open-questions 63 (build decision, was D-CSC-5 in the module draft) | decided (D-184) |
| D-086 | 2026-09-18 | **customer-settings-chat**: Profile hub menu (R-M06): Edit profile, My pets, Payment methods, Notifications, Front Desk chat, App settings, Help & support, Rate the app, About & legal, Log out. Dropped: Add Pets (lives under My pets), Invite Friend (no referral programme), Address (folded into Edit profile). Hub titled "Settings" (tab) and the sub-page "App settings" to end the double "Settings". _Rationale: Slimmer, matches the built variant plus app-store required items._ | open-questions 62 (build decision, was D-CSC-6 in the module draft) | proposed (needs Justin) |
| D-087 | 2026-09-18 | **customer-settings-chat**: Email and password change through separate confirmation flows (modal with current password for email; own screen with strength meter for password) (R-M01). _Rationale: Matches Figma links; security best practice._ | open-questions 15 (build decision, was D-CSC-7 in the module draft) | proposed |
| D-088 | 2026-09-18 | **customer-settings-chat**: Account deletion (R-M21): request from settings, **30-day grace period**, blocked while bookings are requested / pending_vaccines / confirmed / checked_in, cancellable by signing in again, invoices kept anonymised. _Rationale: Apple / Google require in-app deletion; grace period protects active stays and accidental taps._ | open-questions 15 (build decision, was D-CSC-8 in the module draft) | decided (D-184) |
| D-089 | 2026-09-18 | **customer-settings-chat**: Saved cards are tokenised (R-M22): the app stores brand, last4, expiry, holder and a provider token only; one default per customer; cash at the location is always offered. Stripe Elements replaces the card form when the provider is wired. _Rationale: PCI scope; Stripe is on the kanban._ | open-questions 55 (build decision, was D-CSC-9 in the module draft) | proposed |
| D-090 | 2026-09-18 | **customer-settings-chat**: Notification preferences (R-M23): categories bookings, vaccines, chat, payments, offers x channels push, email, SMS; offers default off; receipts / legal / deletion mails always sent. _Rationale: Standard consent model; keeps marketing opt-in explicit._ | - (build decision, was D-CSC-10 in the module draft) | proposed |
| D-091 | 2026-09-18 | **customer-settings-chat**: Notification kinds (R-M07): booking_confirmed, checkin_reminder, booking_status, payment, receipt, refund, vaccine_expiring, vaccine_verified, vaccine_submitted, vaccine_rejected, pet_added, pet_approved, chat, photo, promo, system. Orders / deliveries / vet reminders from the Figma list are not kinds. _Rationale: UI-kit leftovers per open-questions 6._ | open-questions 6 (build decision, was D-CSC-11 in the module draft) | proposed |
| D-092 | 2026-09-18 | **customer-settings-chat**: Relative time grammar (R-M08): Just now, N min ago, N h ago, Yesterday, weekday within 7 days, then "Sep 3" (with year when different). _Rationale: Fixes "3 Hour Ago" inconsistencies._ | open-questions 64 (build decision, was D-CSC-12 in the module draft) | proposed |
| D-093 | 2026-09-18 | **customer-settings-chat**: Chat attachments (R-M28): images only (jpg / png / webp) up to 2 MB, from both sides; stored as data URLs in the mock, real storage with Company-OS. _Rationale: Photos are the main use case (Figma shows a dog photo from the desk)._ | - (build decision, was D-CSC-13 in the module draft) | proposed |
| D-094 | 2026-09-18 | **customer-settings-chat**: Rate the app (R-M27) writes a **pending review** with stars, tags (Excellent / Amazing / Normal / Not good) and comment; the owner moderates (A-35); the store-review prompt is added with Capacitor. _Rationale: Reuses the reviews table and moderation rule R-M12._ | open-questions 111 (build decision, was D-CSC-14 in the module draft) | proposed |
| D-095 | 2026-09-18 | **customer-settings-chat**: Help & support: chat first, then `tel:` call per location, FAQ from a table, and a support request form stored per home location. No voice / video calls in-app (R-M11 requested). _Rationale: Keeps support inside existing channels._ | open-questions 21 (build decision, was D-CSC-15 in the module draft) | proposed |
| D-096 | 2026-09-18 | **customer-settings-chat**: Demo-only staff auto-reply in chat (`chatMock.ts`) so the customer flow can be exercised without a second browser; removed when the staff inbox (F-61) or Company-OS realtime exists. _Rationale: Testing convenience; clearly labelled mock._ | - (build decision, was D-CSC-16 in the module draft) | proposed |
| D-097 | 2026-09-18 | **frontdesk-reservations**: The reservations table groups are **Arriving / Departing / Staying / Daycare / Checked out** for the selected day; "Checking out" from the design is the same as Departing. _Rationale: Open question 69: the design mixed statuses inside DEPARTING; the lifecycle buckets (`dayBucket`, R-I05) are unambiguous._ | open-questions 69, 70 (build decision, was FDR-01 in the module draft) | decided |
| D-098 | 2026-09-18 | **frontdesk-reservations**: Design chips **Future / Checking In / Checked Out / Completed map onto confirmed / checked_in / checked_out**; no second vocabulary (R-I02). _Rationale: D-019 fixed one lifecycle._ | open-questions 70 (build decision, was FDR-02 in the module draft) | decided |
| D-099 | 2026-09-18 | **frontdesk-reservations**: The **v2 table (date navigator, 18 columns) supersedes v1**; "Timesheet View" = the Timeline (F-13); "See All" = the "All upcoming" mode; Filters = status / kind / room type / cancelled toggle + search. _Rationale: Open question 71._ | open-questions 71 (build decision, was FDR-03 in the module draft) | decided |
| D-100 | 2026-09-18 | **frontdesk-reservations**: **Row click opens the booking detail**; per-row quick action is the natural next transition; no bulk actions yet. _Rationale: Open question 72; bulk check-in can come later._ | open-questions 72 (build decision, was FDR-04 in the module draft) | decided |
| D-101 | 2026-09-18 | **frontdesk-reservations**: **Nbr days = nights** (check-out day - check-in day); a same-day stay counts as one night for availability and price. Pet(s) lists every pet, Pet count is the number. _Rationale: Open question 73._ | open-questions 73 (build decision, was FDR-05 in the module draft) | decided |
| D-102 | 2026-09-18 | **frontdesk-reservations**: **Booking detail is hotel-specific** (dates, room, deposits, balance, activity); the tax lines are the real engine lines and are added to the total (design's five placeholder tax lines dropped). _Rationale: Open question 74; R-H06 was a UI-kit placeholder._ | open-questions 74 (build decision, was FDR-06 in the module draft) | decided |
| D-103 | 2026-09-18 | **frontdesk-reservations**: **Timeline bar colour = booking status** (R-I09); flags = vaccine issue, balance due, notes, medication; the "..." chips become truncated labels with a full title. _Rationale: Open question 77._ | open-questions 77 (build decision, was FDR-07 in the module draft) | decided |
| D-104 | 2026-09-18 | **frontdesk-reservations**: The **SORT / FILTER / date-range variant is deprecated**; the Today / arrows / Filters header is used everywhere (ReservationDayNav). _Rationale: Open question 78._ | open-questions 78 (build decision, was FDR-08 in the module draft) | decided |
| D-105 | 2026-09-18 | **frontdesk-reservations**: Room rows are the seeded room codes (PH(B) 101 ... , Suite A1 ...); **Suites group is populated**; Daycare rows are Full day / Half day / Play hour. _Rationale: Open question 81._ | open-questions 81 (build decision, was FDR-09 in the module draft) | decided |
| D-106 | 2026-09-18 | **frontdesk-reservations**: **Board view designed fresh**: columns Requested / Confirmed / In progress / Done / Cancelled-no show, cards "Pet · Breed / Customer / Package · size / minutes / groomer", groomer colour as accent, PIN on the closed column (R-X06). _Rationale: Open question 84; D-008._ | open-questions 84 (build decision, was FDR-10 in the module draft) | decided |
| D-107 | 2026-09-18 | **frontdesk-reservations**: **Phone degradation**: timeline scrolls horizontally with a sticky 96 px room column; board columns stack; tables become cards under 768 px (D-016). _Rationale: Open question 85._ | open-questions 85 (build decision, was FDR-11 in the module draft) | decided |
| D-108 | 2026-09-18 | **frontdesk-reservations**: **Boarding total shown once**; Sub total = rooms + services, then discounts, tax, card fee, Total charge, Deposits, Balance (R-D14). _Rationale: Open question 87._ | open-questions 87 (build decision, was FDR-12 in the module draft) | decided |
| D-109 | 2026-09-18 | **frontdesk-reservations**: **Chargeable days are not a dropdown**: nights drive the price; first / last day multipliers (R-D09) are not modelled. **Needs Justin.** _Rationale: Open question 88; pricing-affecting._ | open-questions 88 (build decision, was FDR-13 in the module draft) | proposed (needs Justin) |
| D-110 | 2026-09-18 | **frontdesk-reservations**: **Type / Handler "All" dropdowns are filters**: Handler = optional employee with is_handler; room type is a radio card. Sales tax = the active taxes row. _Rationale: Open question 89._ | open-questions 89 (build decision, was FDR-14 in the module draft) | decided |
| D-111 | 2026-09-18 | **frontdesk-reservations**: **Additional services**: catalog = `services` rows with category extra; Occurs = once / daily / per night; M / A / E are toggles; stored in `booking_services`. _Rationale: Open question 90._ | open-questions 90 (build decision, was FDR-15 in the module draft) | decided |
| D-112 | 2026-09-18 | **frontdesk-reservations**: **Deposit defaults to 0** on desk bookings; Paid in full sets deposit = total; payments recorded on the detail through the PaymentProvider. **Needs Justin** (deposit amount / percentage, open question 47). _Rationale: No deposit rule exists in the designs._ | open-questions 47 (build decision, was FDR-16 in the module draft) | proposed (needs Justin) |
| D-113 | 2026-09-18 | **frontdesk-reservations**: **Desk bookings start confirmed only when every required vaccine of every pet is verified**, else pending_vaccines (R-X04); confirming anyway needs a manager PIN. _Rationale: R-A05 + R-I06._ | open-questions 34 (build decision, was FDR-17 in the module draft) | decided |
| D-114 | 2026-09-18 | **frontdesk-reservations**: **Hotel guests are not deducted from daycare capacity** (R-E14 working answer). **Needs Justin.** _Rationale: Designer's own question; affects capacity._ | open-questions 60, R-E14 (build decision, was FDR-18 in the module draft) | proposed (needs Justin) |
| D-115 | 2026-09-18 | **frontdesk-reservations**: **Dogs over 30 lb cannot be assigned a top penthouse room** (R-E09 enforced); the 55 lb suite rule (R-X01) only warns until confirmed. _Rationale: R-X01 is `requested`._ | R-E09, R-X01 (build decision, was FDR-19 in the module draft) | decided |
| D-116 | 2026-09-18 | **frontdesk-reservations**: **Check-in requires a room**; checked-in stays keep their dates but can change room (R-X05); a timeline move keeps the nights (R-X07). _Rationale: Operational sanity for the desk._ | built fresh (build decision, was FDR-20 in the module draft) | decided |
| D-117 | 2026-09-18 | **frontdesk-reservations**: **Booking codes** `PR-<n>` continue from the highest existing number; every action writes `booking_events` (R-J08). _Rationale: Replaces the legacy "Added / edited by" line._ | R-J08 (build decision, was FDR-21 in the module draft) | decided |
| D-118 | 2026-09-18 | **frontdesk-grooming-people**: Groomer column order, colour and hidden state are saved **per staff user and location** (`groomer_column_prefs`), not globally. _Rationale: Groomers and desk staff read the grid differently; a global change would surprise colleagues. Employee colour stays the default._ | open-questions 82 (build decision, was P-FGP-01 in the module draft) | decided |
| D-119 | 2026-09-18 | **frontdesk-grooming-people**: Day view title is **"Grooming day view"**; hours run from the location's opening hour -1 to closing +1, slots on 15 minutes; the stray "01:30 PM" row in Figma is dropped. _Rationale: Grooming.png vs Grooming.pdf title conflict; the extra row is a design error._ | open-questions 79 (build decision, was P-FGP-02 in the module draft) | decided |
| D-120 | 2026-09-18 | **frontdesk-grooming-people**: Agenda row flags mean **warning = unconfirmed or has a note, coin = payment pending, red cross = vaccine issue**; a row opens the appointment detail (F-34). The "+ Hotel Reservation" button is not shown on grooming pages. _Rationale: Figma placeholders; the hotel CTA on a grooming page is inconsistent._ | open-questions 83 (build decision, was P-FGP-03 in the module draft) | decided |
| D-121 | 2026-09-18 | **frontdesk-grooming-people**: Appointments keep the core enum `requested / confirmed / in_progress / done / cancelled / no_show`; transitions and PIN gates **mirror the booking lifecycle** (cancel / no-show a confirmed one and re-open need a manager PIN; start / finish do not). _Rationale: One vocabulary per entity, one rulebook for approvals (R-I06, D-019)._ | open-questions 70, 104 (build decision, was P-FGP-04 in the module draft) | decided |
| D-122 | 2026-09-18 | **frontdesk-grooming-people**: Several pets on one groom visit = **one appointment row per pet** at the same time and groomer (R-G16). _Rationale: Day view, board and invoices stay per pet; matches the Figma pet table with add-row._ | Groom Booking .pdf (build decision, was P-FGP-05 in the module draft) | decided |
| D-123 | 2026-09-18 | **frontdesk-grooming-people**: **Recurring groom bookings are not built** (R-G18 requested). Groom Style is kept as free text on the pet profile and the appointment. _Rationale: Redesign dropped recurrence; legacy "Use Groom Style" only needs a saved text. **needs Justin**_ | open-questions 91 (build decision, was P-FGP-06 in the module draft) | proposed (needs Justin) |
| D-124 | 2026-09-18 | **frontdesk-grooming-people**: Pet form has an **owner selector** (prefilled from the customer page / `?customer=`) so pets can also be created from the pets list. _Rationale: Figma has no owner field because it opens from a customer; the list needs one._ | open-questions 29 (build decision, was P-FGP-07 in the module draft) | decided |
| D-125 | 2026-09-18 | **frontdesk-grooming-people**: Customer and pet **Status = active / inactive** only; "delete" is a soft deactivate behind a manager PIN (R-X68). _Rationale: Bookings and invoices keep referencing the row._ | open-questions 30 (build decision, was P-FGP-08 in the module draft) | decided |
| D-126 | 2026-09-18 | **frontdesk-grooming-people**: Front-desk vaccine table shows **all five vaccine types** (Rabies, DHPP, Bordetella required; Lepto, Influenza recommended) - the canonical list from D-019 - not the Figma four. _Rationale: One list across surfaces._ | open-questions 31, 37 (build decision, was P-FGP-09 in the module draft) | decided |
| D-127 | 2026-09-18 | **frontdesk-grooming-people**: Desk-entered vaccine with dates **and** a certificate by a user holding `vaccines.verify` is stored **verified** on entry; dates only = submitted; nothing = missing (R-X65). Rejecting a proof needs a reason and notifies the customer (R-X61). _Rationale: The desk holds the paper; the app upload path still goes through verification._ | open-questions 34 (build decision, was P-FGP-10 in the module draft) | decided |
| D-128 | 2026-09-18 | **frontdesk-grooming-people**: Verifying the last required vaccine **approves the pet and auto-confirms `pending_vaccines` hotel / daycare bookings whose pets are all approved** (R-X60), with audit and a customer notification. _Rationale: The brief makes vaccines a first-class gate; staff should not have to confirm twice. **needs Justin** (auto-confirm vs manual review)_ | open-questions 34 (build decision, was P-FGP-11 in the module draft) | proposed (needs Justin) |
| D-129 | 2026-09-18 | **frontdesk-grooming-people**: Inbox improvements beyond Figma (D-018): **Mine / Unassigned / Open / Closed filters, assignment to a colleague, close / reopen**; voice / video call icons dropped (R-M11 requested). Timestamps relative under a week (R-M08). _Rationale: D-018 allows it; telephony has no provider._ | open-questions 6, 66 (build decision, was P-FGP-12 in the module draft) | decided |
| D-130 | 2026-09-18 | **frontdesk-grooming-people**: Invoice numbering from `settings.invoice.next_number` (INV-####), footer from the same setting; an invoice can be created from any booking, appointment or daycare day; "Take payment" goes through the PaymentProvider with a fixed mock test card. _Rationale: 9.pdf invoice settings; keeps Stripe a drop-in._ | open-questions 74, 97 (build decision, was P-FGP-13 in the module draft) | decided |
| D-131 | 2026-09-18 | **frontdesk-grooming-people**: Grooming capacity stays **2 simultaneous per location** even with 3 seeded Encino groomers; over-capacity hours are shaded red but saving is allowed with a warning. _Rationale: R-G21 / R-E10 value from front desk-7; whether capacity should equal groomers on shift is a business call. **needs Justin**_ | open-questions 60 (analogue), front desk-7 (build decision, was P-FGP-14 in the module draft) | proposed (needs Justin) |
| D-132 | 2026-09-18 | **frontdesk-grooming-people**: `/desk/notifications` is served by this module as **F-60** (the foundation stub used F-60). _Rationale: The TopBar bell already links to this path and it belongs with the inbox in "Messages & reviews". Integrator to reconcile with the extras module._ | build-plan ranges (build decision, was P-FGP-15 in the module draft) | decided |
| D-133 | 2026-09-18 | **admin-control-panel**: One Settings area for the owner (A-1x..A-4x under the admin surface); front desk pages read the same tables and never carry their own settings screens. Tab names from 12-14.pdf (Spa / Hotel & Daycare / Employee setup) are replaced by task pages: Pricing (A-27 with sub-pages), Services (A-26), Employees (A-30). _Rationale: D-002 makes Control Panel owner-only; one source avoids two copies of pricing._ | Q94 (build decision, was D-pending-ACP-01 in the module draft) | proposed |
| D-134 | 2026-09-18 | **admin-control-panel**: Each modal gets its own title (Company profile, Location details, Employee) instead of the shared "Name And Contact Details". _Rationale: Copy error in 2/3/15.pdf._ | Q95 (build decision, was D-pending-ACP-02 in the module draft) | proposed |
| D-135 | 2026-09-18 | **admin-control-panel**: Boarding settings keep one "book out whole room by default" toggle; the duplicate in 4.pdf is dropped until Justin names the second option. _Rationale: Duplicate field._ | Q96 (build decision, was D-pending-ACP-03 in the module draft) | proposed |
| D-136 | 2026-09-18 | **admin-control-panel**: Invoice settings: "Boarding Hours" section label ignored; Payment Processing Option 1-3 replaced by the PaymentProvider seam card on A-28 (mock now, Stripe later). _Rationale: Placeholders in 9.pdf; Stripe is on the kanban._ | Q97 (build decision, was D-pending-ACP-04 in the module draft) | proposed |
| D-137 | 2026-09-18 | **admin-control-panel**: Package calendar minutes seeded per size (Gold 60/60/60/90/90, Platinum 75/75/90/105/120, Diamond 90/105/120/135/150); Platinum prices from the Spa 12.7 card; Diamond prices placeholders. All editable on A-24. _Rationale: 12.pdf time cells contain dollar values._ | Q98 (build decision, was D-pending-ACP-05 in the module draft) | proposed (needs Justin) |
| D-138 | 2026-09-18 | **admin-control-panel**: Add-on "+" = `starting_at` flag (final price depends on coat condition); the duplicate Color/Highlights row is seeded once at $15; "special employee" is a free-text `employee_type` on the add-on, editable on A-25. _Rationale: Two entries and an undefined taxonomy._ | Q99 (build decision, was D-pending-ACP-06 in the module draft) | proposed (needs Justin) |
| D-139 | 2026-09-18 | **admin-control-panel**: Capacity table's second "LOS ANGELES" column treated as a typo; capacities are per location and editable on A-11; a third location is added through A-41 when needed. _Rationale: No third location exists._ | Q100 (build decision, was D-pending-ACP-07 in the module draft) | proposed |
| D-140 | 2026-09-18 | **admin-control-panel**: Seasonal rates are date-range configurable (seasons table with holiday flag, A-20); holidays per location are configurable on A-11 and exclude long-stay discounts. _Rationale: R-D06, R-E15._ | Q101 (build decision, was D-pending-ACP-08 in the module draft) | proposed |
| D-141 | 2026-09-18 | **admin-control-panel**: Settings cards use a drawer (AdminRecordDrawer) for add / edit; delete exists and is PIN-gated (R-X42); no inline table editing. _Rationale: One consistent form; deletes audited._ | Q102 (build decision, was D-pending-ACP-09 in the module draft) | proposed |
| D-142 | 2026-09-18 | **admin-control-panel**: "General User Security Setting" is per role: the permissions matrix (A-31) edits the `permissions` table; the manager PIN is separate (approvals) and its length stays 4-6 digits (D-019). _Rationale: 10.pdf lists permissions without a subject; roles are the unit elsewhere._ | Q103, Q104 (build decision, was D-pending-ACP-10 in the module draft) | proposed |
| D-143 | 2026-09-18 | **admin-control-panel**: Default location hours Mon-Fri 7:00-19:00, Sat-Sun 9:00-17:30 (R-K02); the 07:45 PM dropdown defaults in the PDFs are placeholders. _Rationale: Consistent with 1.pdf / front desk-7._ | Q105 (build decision, was D-pending-ACP-11 in the module draft) | proposed |
| D-144 | 2026-09-18 | **admin-control-panel**: Providers: email None / Gmail / SMTP, SMS None / Twilio / Petlinx (legacy), push None / FCM / APNs; all stubs with test-send logging only. _Rationale: Platform integrations replace PetLinx; nothing sends until Company-OS._ | Q106 (build decision, was D-pending-ACP-12 in the module draft) | proposed |
| D-145 | 2026-09-18 | **admin-control-panel**: Employee record = one `employees` row with operational fields (job, department, status, colour, groomer / handler flags, PIN, working hours JSON) linked to a `users` row for login and role; HR tabs (payroll, performance, documents) are out of scope. _Rationale: D-005 employees = user management._ | Q108, Q109 (build decision, was D-pending-ACP-13 in the module draft) | proposed |
| D-146 | 2026-09-18 | **admin-control-panel**: Department is a short fixed list (Operations, Receptionist, Groom, Take Care, Sales); "On leave" is set manually; phone default +1. _Rationale: Design list mixed titles and departments._ | Q110 (build decision, was D-pending-ACP-14 in the module draft) | proposed |
| D-147 | 2026-09-18 | **admin-control-panel**: Review moderation: pending -> published or archived; a published review can go back to pending or be archived (unpublish). Tags come from the customer app; ratings one decimal. _Rationale: R-M12; answers Q111._ | Q111 (build decision, was D-pending-ACP-15 in the module draft) | proposed |
| D-148 | 2026-09-18 | **admin-control-panel**: Reports use booked value (totals of confirmed / in-house / completed records) rather than cash received; the dashboard revenue tile uses paid payments. CSV export per report tab. _Rationale: Cash vs booked both useful; labelled on each page._ | Q75 (build decision, was D-pending-ACP-16 in the module draft) | proposed (needs Justin) |
| D-149 | 2026-09-18 | **admin-control-panel**: Per-role side menus are stored in `roles.menu` (category -> visible), edited on A-32; Overview cannot be hidden. DesktopShell reads it after integration (shared file). _Rationale: D-014._ | - (build decision, was D-pending-ACP-17 in the module draft) | proposed |
| D-150 | 2026-09-18 | **admin-control-panel**: New owner rules for this module use ids R-X40..R-X49 (the A-4x range) to avoid clashes with other module builders' R-Xnn ids. _Rationale: Parallel builders cannot coordinate ids._ | - (build decision, was D-pending-ACP-18 in the module draft) | proposed |
| D-151 | 2026-09-18 | **extras-manual-website**: The rebuilt website lives at `/#/site/home`, `/#/site/hotel`, … (P-01..P-12) beside the P-00 placeholder at `/#/site`; the integrator repoints `/site` to P-01. _Rationale: The public module owns `/site`; registering the same path from two modules would warn and depend on module name order._ | build plan: website rebuild P-01..P-19 (build decision, was D-P01 in the module draft) | decided |
| D-152 | 2026-09-18 | **extras-manual-website**: Website prices are teasers ("from …") computed from the pricing tables through the same helpers as the app; the binding quote is in the app (R-X71). _Rationale: Never hardcode a price; the site must not disagree with the estimate._ | project brief pricing rule (build decision, was D-P02 in the module draft) | decided |
| D-153 | 2026-09-18 | **extras-manual-website**: Only reviews with `status = published` appear on the website; F-65 Approve = published, Archive = archived (R-X70, R-M12). Two sentiment tags per review are kept as free `tags`. _Rationale: Open question 111 (does Approve = Publish?)._ | open-questions.md 111 (build decision, was D-P03 in the module draft) | decided |
| D-154 | 2026-09-18 | **extras-manual-website**: Website contact form writes `site_inquiries` at the visitor's chosen location; the desk handles them in F-61 with statuses new / seen / replied / closed; reply target one business day (R-X72, requested). **needs Justin** _Rationale: No inquiry channel existed in the designs; the desk already owns messages._ | project brief (website rebuild) (build decision, was D-P04 in the module draft) | proposed (needs Justin) |
| D-155 | 2026-09-18 | **extras-manual-website**: FAQs are a table (`site_faqs`, topic, sort_order, published) edited in the table manager, not code. _Rationale: Owners change copy without a deploy._ | - (build decision, was D-P05 in the module draft) | decided |
| D-156 | 2026-09-18 | **extras-manual-website**: Ops manual chapters are markdown with front matter (title, code, roles, part, version, updated, summary, rules) under `docs/ops-manual/en/`; each chapter is its own route and page code (M-10..M-27); M-01 cover, M-02 fallback, M-03 pending. Parts I–VI as listed in `docs/ops-manual/README.md`. _Rationale: hoy pattern (docs/reference/hoy-patterns.md section 6) adapted to one code per chapter so specs, screenshots and page docs exist per chapter._ | project brief (business operations manual) (build decision, was D-P06 in the module draft) | decided |
| D-157 | 2026-09-18 | **extras-manual-website**: Live data in chapters comes only from `LiveBlock` directives; a typed number in a chapter is a bug (R-X73). _Rationale: Manual must stay true when settings change._ | hoy rule (build decision, was D-P07 in the module draft) | decided |
| D-158 | 2026-09-18 | **extras-manual-website**: Each chapter has an in-person and an online lesson; staff record both, managers sign off in-person in F-66; stored in `training_completions` per employee per chapter per mode (R-X74). _Rationale: The brief asks for in-person and online lessons per role._ | project brief (build decision, was D-P08 in the module draft) | decided |
| D-159 | 2026-09-18 | **extras-manual-website**: Manual audience labels: all staff, front desk, groomer, manager, owner; role -> audience mapping in `manualIndex.ts` (manager also reads front desk chapters, owner also reads manager chapters, super admin reads all). Manual routes are open to all staff roles; customers do not see the manual. _Rationale: The stub gave ALL_SIGNED_IN; a staff operations manual is not customer-facing._ | stub M-01 (build decision, was D-P09 in the module draft) | decided |
| D-160 | 2026-09-18 | **extras-manual-website**: Reports: periods 7 / 30 / 90 days ending today; collected = payments with status paid by paid_at; service from invoices.source_type; occupancy excludes cancelled and no_show; money needs `reports.financial` (owner), counts are for managers (R-X76). _Rationale: Reports have no design (empty shell); permissions from R-L05._ | D-005, report.jpg (build decision, was D-P10 in the module draft) | decided |
| D-161 | 2026-09-18 | **extras-manual-website**: Reviews moderation F-65 is the front-desk/manager queue; the admin module's A-35 can reuse `ReviewListItem` and the same table (no second component). _Rationale: "reviews moderation reuse" in the module scope._ | reviews.jpg (build decision, was D-P11 in the module draft) | decided |
| D-162 | 2026-09-18 | **extras-manual-website**: Walking (F-67) is a real walk log (`walks`: pet, handler, start, minutes, status, potty, note) rather than an empty placeholder; billing the daycare "Walk" item from walks is deferred (R-F05). **needs Justin** (is Walk $12 a daycare add-on or a separate service?) _Rationale: Open question 61._ | open-questions.md 61 (build decision, was D-P12 in the module draft) | proposed (needs Justin) |
| D-163 | 2026-09-18 | **extras-manual-website**: Management (F-68) = tasks and checklists per location (`tasks.kind`); Employees > Check List from the Figma nav is a checklist kind, not a separate page (R-L07). Delete is PIN-gated (R-X77). _Rationale: Empty shells in the design; R-P01 makes deletes approval-gated._ | education-1.jpg, employees-1.jpg (build decision, was D-P13 in the module draft) | decided |
| D-164 | 2026-09-18 | **extras-manual-website**: Notification kinds are a fixed vocabulary (R-M07): booking_confirmed, booking_requested, payment, vaccine_expiring, vaccine_submitted, pet_added, message, review, feedback, system; UI-kit leftovers (orders, deliveries, vet reminders) are dropped. Staff notifications are per user and marked read on open (R-X78). _Rationale: Open question 6 (placeholder leftovers)._ | open-questions.md 6 (build decision, was D-P14 in the module draft) | decided |
| D-165 | 2026-09-18 | **extras-manual-website**: Voice / video call icons in the chat design are not built (R-M11 requested); images on messages are supported by the schema. _Rationale: Scope open in the design._ | R-M11 (build decision, was D-P15 in the module draft) | decided |
| D-166 | 2026-09-18 | **extras-manual-website**: Owner rules first registered by this module use ids R-X70..R-X78 to leave R-X03..R-X39 to other modules; the integrator renumbers on collision. _Rationale: Parallel modules cannot coordinate ids._ | CLAUDE.md rules contract (build decision, was D-P16 in the module draft) | decided |
| D-167 | 2026-09-18 | **dev-quality**: Dev-quality rules use the id band **R-X80..R-X89**; other modules should take R-X04.. upward _Rationale: Parallel module agents add owner rules as R-Xnn; a reserved high band avoids id collisions at merge time_ | CLAUDE.md "new owner rules are R-Xnn" (build decision, was DQ-1 in the module draft) | proposed |
| D-168 | 2026-09-18 | **dev-quality**: **Performance budgets are rows** in `perf_budgets` (js_total 500 kB gzip, largest chunk 450 kB, css 60 kB, docs 1.2 MB, localStorage 3.5 MB, seed 400 ms, first render 1.5 s, 220 routes, 120 assets), warn at 80 % _Rationale: Same principle as pricing: limits in tables, pages compute. The app ships as one chunk today (415 kB gzip) so the chunk budget equals the total; code-splitting per surface is the lever when it trips_ | build-plan phase 3 (build decision, was DQ-2 in the module draft) | proposed |
| D-169 | 2026-09-18 | **dev-quality**: **Historical seed** covers check-outs from 365 to 22 days ago with `quote: null` on bookings (invoice keeps the lines); own PRNG stream (seed 20250918) _Rationale: Reports need a year; keeping quotes off historical rows holds the localStorage snapshot at ~1.5 MB of the ~5 MB origin cap; a separate stream keeps core's "today" data stable_ | dev-quality scope (build decision, was DQ-3 in the module draft) | proposed |
| D-170 | 2026-09-18 | **dev-quality**: **Layout adoption is opt-in**: pages call `useLayout(spec)` from `src/layout/useLayout.ts`; the spec stays the source of section names and new sections append after saved ones _Rationale: No shared-file edit needed; foundation pages adopt when they are next touched_ | hoy pattern (build decision, was DQ-4 in the module draft) | proposed |
| D-171 | 2026-09-18 | **dev-quality**: **A11y checks are heuristics with exemptions** (visually-hidden native inputs, checkbox / radio / range / file, inline links, controls in tables; `target-size` and `heading-skip` are warnings) _Rationale: Keeps the QA script's exit code about layout and crashes, while still surfacing WCAG 2.5.8 / 1.3.1 gaps_ | WCAG 2.2 AA (build decision, was DQ-5 in the module draft) | proposed |
| D-172 | 2026-09-18 | **dev-quality**: **QA scripts spawn their own preview server and refuse a busy port** (`--port`, `QA_PORT`, `QA_NO_SERVER=1` to reuse one) _Rationale: Several agents run previews on one machine; the first run silently captured another worktree's build on a shared port_ | incident during this build (build decision, was DQ-6 in the module draft) | proposed |
| D-173 | 2026-09-18 | **dev-quality**: The docs module got a **two-line edit** (sidebar title filter + link to D-18) and the D-04 table manager a **one-line link** to the workbench _Rationale: Discoverability; both are foundation modules, not parallel module agents' files, so merge risk is nil; flagged in notes_for_merge_ | CLAUDE.md "never edit another module's files to add a page" (build decision, was DQ-7 in the module draft) | needs integrator ack |
| D-174 | 2026-09-18 | **dev-quality**: **CSV imports of more than one row need a manager PIN**; a single-row import behaves like Add row _Rationale: Bulk changes are as risky as deletes (R-P01); one row is an ordinary edit_ | R-P01 (build decision, was DQ-8 in the module draft) | proposed (needs Justin) |
| D-175 | 2026-09-18 | **QA fixes**: **12 px is the floor for visible text** (`--fs-2xs` = 12 px; count badges use an 18 px box; `code` / `small` are clamped with `max()`); chart tick labels included _Rationale: the D-016 matrix flagged text under 12 px on 178 routes; one token change plus the hard-coded 10 / 11 px removed fixes it everywhere_ | docs/qa/responsive-report.md, changelog 0019 | decided |
| D-176 | 2026-09-18 | **QA fixes**: **One staff shell** for front desk and admin surfaces: the side menu is built from admin + frontdesk + manual routes filtered by `hasRole`, and the shell title follows the role (Owner / admin, Manager, Front desk, Grooming) _Rationale: a manager's menu must be complete on every page (D-014); the menu never changes with the current route_ | docs/qa/roles-report.md finding 2 | decided |
| D-177 | 2026-09-18 | **QA fixes**: **Pinned staff never open the other location's records by URL**: F-12, F-52, F-55 compare the row (or the owner's home location) with the pinned location and render the not-found state; approvals carry the subject row's `location_id` _Rationale: list pages were scoped but detail pages were not; the audit table must attribute approvals to the booking's location_ | docs/qa/roles-report.md finding 1 | decided |
| D-178 | 2026-09-18 | **QA fixes**: **`bookings.quote` is read through `quoteLinesOf()`** (`src/domain/booking.ts`), which accepts the desk / seed `Quote` and the app's `{ hotel, grooming, fee }` bundle; every route sits inside an `ErrorBoundary` so a page error shows a fallback instead of a white screen _Rationale: the e2e blocker (F-12 crash for app bookings) came from two quote shapes; normalising on read keeps both writers untouched_ | docs/qa/e2e-report.md finding 1 | decided |
| D-179 | 2026-09-18 | **QA fixes**: **Check-in / check-out stay PIN-free** (`PIN_GATED_TRANSITIONS` unchanged) until Justin decides whether confirmed -> checked_in needs a manager PIN _Rationale: the QA brief expected a PIN at check-in; the desk flow was designed without one so the morning rush is one click_ | docs/qa/e2e-report.md finding 8 | proposed (needs Justin) |
| D-180 | 2026-09-18 | **QA fixes**: **The mock database keeps runtime rows across the daily reseed** (ids from `newId()` are carried over; edits to seeded rows reset) and **`MockProvider` writes replace the table array** so `useTable` subscribers always get a new reference _Rationale: demo data created by a person disappeared at midnight, and in-place mutation left F-56 stale until reload_ | docs/qa/e2e-report.md findings 2 and 9 | decided |
| D-181 | 2026-09-18 | **QA fixes**: **Capacity rules R-E11 / R-E12 are informational** (rooms drive hotel availability, R-X03); **R-E13 daycare capacity is `in_dev`** until the daycare desk pages (F-40..F-49) enforce it; the customer daycare flow does not block over capacity yet _Rationale: the coverage report showed `implemented` rules that no booking flow checks_ | docs/qa/coverage-report.md finding 5 | decided |
| D-182 | 2026-09-18 | **QA fixes**: **QA scripts follow the D-016 brief**: customer routes run as the customer demo user with seeded wizard drafts, staff routes as the super admin with dev mode off; `qa-responsive.mjs` fails a cell on horizontal scroll, console error, text under 12 px, a fixed element over a stuck sticky element or a blank page, and records redirects _Rationale: the previous report measured the redirect target for 11 wizard steps and did not check the brief's criteria_ | docs/qa/responsive-report.md, scripts/qa-lib.mjs | decided |
| D-183 | 2026-09-18 | **Company-OS is reference only.** Nothing in Petrock writes to, calls, or wires into Playset-LLC/Company-OS until Justin says so; the project is separate for now and its developer will connect it when ready. `CompanyOsProvider` stays an empty adapter seam (the mock `DataProvider` remains the runtime). The REST-API / BFF recommendation in `docs/reference/company-os.md` is parked, not decided. | prompt 0008, Justin answer to Q18 | decided |
| D-184 | 2026-09-18 | **Auth policy is defined in Supabase Auth later.** Password rules (min 8 chars, letter + number) and lockout (5 failures / 15 min) in D-024 / D-025 are mock-only working values; the real policy will be configured in Supabase Auth when the app moves there (its password requirements and rate limits replace the mock's). App-side items stay as built: 30-day account-deletion grace (D-088) and English + Spanish only (D-085). | prompt 0008, Justin answer to Q14 | decided (values to be set in Supabase later) |
| D-185 | 2026-09-18 | **Suites are smaller than penthouses.** Suite inclusions = penthouse minus the TV: "Premium bed, toys during the day, potty pads, room service, playtime, 2 walks per day, photos and videos every night of the stay, bedtime tuck-in and tummy rub." (room copy per the petrockhotel.com/hotel listing, resolves R-D03); supersedes the invented Suite copy in D-067. Seeded in `room_types.description`, never in a page. | prompt 0008, Justin 13:45 UTC ("suites are smaller than penthouses") + petrockhotel.com/hotel (indexed copy) | decided |
| D-186 | 2026-09-18 | **Penthouses are themed and named.** Each penthouse has a theme (music genre or art style, e.g. rock, pop, Andy Warhol). Rooms carry the theme name in the system, never only a number (e.g. "PH 105 · Rock"). Requires a `display_name` / `theme` column on `rooms` and the per-location name list from the owner (owner questions 1-2 in docs/reference/petrockhotel-site.md); until then room codes stay as seeded (`PH(B) 101`, `Suite A1`). | prompt 0008, Justin 13:45 UTC | decided (names pending owner) |
| D-187 | 2026-09-18 | **petrockhotel.com is the first source for pricing and business facts**; the app seed follows the site, and only the gaps go to the owner. On 2026-09-18 the site was network-blocked from the agent (proxy allowlist), so facts came from indexed copies and public listings (prompt 0008; digest in docs/reference/petrockhotel-site.md). Adopted: Westwood address 10946 Santa Monica Blvd 90025 and its own hours (M-F 8-18, Sa-Su 8-17:30), real phones / SMS lines / emails for both locations, Suite copy (D-185), $12 grooming sanitation fee as a `fees` row that is included in the groom price (quote totals unchanged), the estimate disclaimer on the grooming checkout (C-54). | prompt 0008, Justin 13:45 UTC ("look at petrockhotel.com 1st for accurate data") | decided (verify against the live site when reachable) |
| D-188 | 2026-09-18 | **Open Sans replaces Inter as the app family; Be Vietnam Pro is the display face.** The Figma mobile screens are Open Sans (≈70 of ≈85 deep text nodes), Be Vietnam Pro carries auth titles, profile / pet names and the front-desk detail cards; the build used Inter everywhere ("Inter only", D-007 / tokens-draft), which is why every screen read "off". Both load from `@fontsource` (400/600/700 and 400/500/600, metric fallbacks in `global.css`); `--font-sans` / `--font-display` in `tokens.ts`. Implemented in fidelity part (a); supersedes the "Inter only" line of D-007; numbered D-185 in the audit before D-185..D-187 were taken by prompt 0011 (site facts). _Rationale: Justin asked for the exact Figma styling at the design-system level (prompt 0009/0010)._ | prompt 0009, audit 1.3; prompt 0010 | proposed (implemented, needs Justin) |
| D-189 | 2026-09-18 | **Customer home follows `Home Page.png`**: hero photo with the logo mark, "Services" band with four tiles (Hotel, Spa, Daycare, In Home), "Pets" strip with the selected-pet border and the "Add a Pet" card, "Upcoming Bookings" cards, underlined "View Past Reservations"; **no greeting, no location chip, no bell, no section counts**. The location switch and the notification bell move to Settings / the paw tab (Figma has no location switch on home). Vaccine attention stays as a `#FF6868` status line under the pet card (business rule R-A04), not a yellow banner. | prompt 0009, audit 4 (C-10) | proposed (default applied by part b) |
| D-190 | 2026-09-18 | **Add Pet wizard stepper renders the Figma filled circles without labels** (27 px primary pointers, 2 px track). The wizard keeps its five internal steps (Basics, Details, Care, Vaccines, Review) but the Figma frames show two pointers; default = five filled circles, Justin to confirm two groups vs five. | prompt 0009, audit 4 (C-12) | proposed |
| D-191 | 2026-09-18 | **Hotel booking flow shows no stepper** (Figma `Hotel Reservation.png`, `Choose Your Room.png`, `Booking Detail.jpg`, `Payment.png` have none); progress is the header title per screen. Default = no stepper on C-30..C-36; the compact stepper stays available for the grooming / daycare flows until their frames say otherwise. | prompt 0009, audit 4 (C-30..C-36) | proposed (default applied by part b) |
| D-192 | 2026-09-18 | **Company-OS integration architecture (proposed)**: Supabase Auth for customer and staff identity (D-184); Petrock's booking, pricing, PIN-approval, payments and customer-auth logic contributed as reusable Company-OS modules (no Petrock-owned server, per Justin 15:24 UTC); Company-OS as system of record through its REST API, Petrock = one tenant `petrock` with one org_unit per location; 74 tables -> entities via a generated template; phased 0-4 with read-only first. D-183 stays until Justin confirms Phase 1. Plan: docs/reference/company-os-integration-plan.md. | prompt 0011 | proposed (needs Justin) |
| D-193 | 2026-09-18 | **Quality bar: it must work on 4K TVs as well as phones.** The responsive definition of done (D-016) covers 10-foot UI: legible body text, visible focus and selection from a distance, no layout pinned to a design width. Principle P-01 in `docs/platform-principles.md`. | prompt 0014, Justin 16:09 UTC ("it needs to work on 4k tvs also") | binding (Justin 2026-09-18) |
| D-194 | 2026-09-18 | **TV widths and 10-foot rules**: the D-016 matrix adds **2560 and 3840** px (key pages first: HUB-01, C-10, C-30, F-01, F-10, F-12, A-01, P-01, then all routes); body text >= 16 px at >= 1920; a `--scale` custom property steps type and spacing up at >= 2560; focus ring >= 3 px in a high-contrast token. `scripts/qa-responsive.mjs` adds the widths and a legibility check. _Rationale: implementation of D-193; the exact widths and the scale mechanism are choices, hence proposed._ | prompt 0014 (implementation choice) | proposed |
| D-195 | 2026-09-18 | **Input modalities**: keyboard, mouse, trackpad, touch and pen are expected **now** (focus order, visible focus, 44 px targets, hover never the only affordance, no drag-only interaction); TV remote / gamepad d-pad and voice are expected in the **near future** and nothing is designed against them (spatial-focus-friendly grids, one primary action per screen, named actions). P-03 / P-04. `target-size` becomes an error in the a11y checks (was a warning, D-171). | prompt 0014, Justin | binding (Justin 2026-09-18) |
| D-196 | 2026-09-18 | **Every change updates the control and voice abilities.** A page's buttons, menu items and submits are its actions; adding or removing one updates the page's action list in the same commit, so a voice / agent controller can enumerate and drive the UI. Voice is expected to be fast and real time, operate on screen alongside the person or on its own, through the same actions and `DataProvider`, never a private path. P-05 / P-06. | prompt 0014, Justin ("Everything change needs to update voice and control abilities accordingly"; voice paragraph) | binding (Justin 2026-09-18) |
| D-197 | 2026-09-18 | **`actions` manifest shape**: `PageSpec.actions: { id: '<module>.<verb>', label, intent, permission?, params? }[]`; pages register `run(id, params)` handlers on an actions bus while mounted; `/#/dev/actions` = **D-20** lists every action with page, permission and live handler; `specCompleteness` adds an `actions` check; the registry is the WebMCP surface (one tool per action). _Rationale: implementation of D-196._ | prompt 0014 (implementation choice) | proposed |
| D-198 | 2026-09-18 | **Table management, design-system management and the component library are first-class in the product**: every table in the schema registry and editable in D-04 / D-10, every design value a token shown in D-01, every component with a meta in D-02 / D-08; pages never hand-roll UI; the builder tool stays on every page. Restates the CLAUDE.md contract as a principle (P-07). | prompt 0014, Justin ("Table management, and Design System managment is key. Keeping Components organized as library is key.") | binding (Justin 2026-09-18) |
| D-199 | 2026-09-18 | **Annotations and Submissions live in the product.** Testers comment, request and report bugs on the running product, pinned to elements; records go to the stored system where an agent checks them, decides fix vs ask the developer according to **who** wrote it, records the decision, then changes things. P-08. | prompt 0014, Justin | binding (Justin 2026-09-18) |
| D-200 | 2026-09-18 | **Annotations implementation**: extend the `feedback` table (not a new table) with `kind` (comment / request / bug), `element_path`, `component`, `viewport`, `theme`, `screenshot_url`, `triage` (fix / ask / wontfix), `triage_note`, `decision_ref`; FeedbackButton gains an element picker; pins render for dev mode or `feedback.read`; A-36 stays the one inbox with the new columns and filters; triage workflow written in `docs/reference/annotations-triage.md`. _Rationale: one inbox, one table, same permissions._ | prompt 0014 (implementation choice) | proposed |
| D-201 | 2026-09-18 | **Placeholder and undeveloped UI must announce itself**: any control that is not wired shows a rollover / tooltip and gives an explicit "not wired yet" response on activation, so UI that exists but does not function is always knowable; togglable with developer mode (always visible in dev mode). P-09. | prompt 0014, Justin ("if you put ui in place, and it doesnt actually function, we have a way to know") | binding (Justin 2026-09-18) |
| D-202 | 2026-09-18 | **`Placeholder` atom design**: `src/components/atom/Placeholder/` wraps or badges a control: hover / focus tooltip "Not wired yet - <what it will do>", info toast on activation, dashed outline + badge when `devMode`, subtle tooltip otherwise; a `data-placeholder` attribute so D-09 and the QA scripts can count them; `PageStub` and every toast-only `onClick` (HomePage, PetProfilePage, ...) migrate to it; `spec.notes` names the module that will build the real thing. _Rationale: implementation of D-201._ | prompt 0014 (implementation choice) | proposed |
| D-203 | 2026-09-18 | **MCP / WebMCP, CLI and API surfaces are recorded every pass** in `docs/reference/surfaces.md` (route manifest, `DataProvider` methods, scripts and flags, actions, HTTP API, planned tools); a new surface without its line is incomplete. P-10. Initial version written this turn. | prompt 0014, Justin ("need to be well organized and recorded at every pass") | binding (Justin 2026-09-18) |
| D-204 | 2026-09-18 | **The context and memory system is the docs tree and it is always current**: `docs/README.md` (start-here map), kanban, decisions, changelog, prompts, page docs, build plan and `platform-principles.md`, updated in the same turn as the work; any agent or developer must see the big picture and the details with ease. P-11. | prompt 0014, Justin | binding (Justin 2026-09-18) |
| D-205 | 2026-09-18 | **Hub and the standard deliverable batch**: a project ships a public website, a customer app, staff / admin dashboards, in-app documentation, an operations manual (in person and in software), dev / builder tools and a testing hub; **until real auth is connected and the product is live, the viewer can see everything as any role, with dev mode on / off** (guards stay real, identity is mocked). P-12. | prompt 0014, Justin | binding (Justin 2026-09-18) |
| D-206 | 2026-09-18 | **English and Spanish toggle from the start**: every visible string through `useT()` with `{ en, es? }` in the module's `strings`; Spanish falls back to English; the Spanish fill is a pass, hard-coded English in JSX is a defect. Extends D-085 (en + es only). P-13. | prompt 0014, Justin ("Multilingual Support, starting with english and spanish toggle") | binding (Justin 2026-09-18) |
| D-207 | 2026-09-18 | **Multiplayer and realtime are expected, not first pass, and never designed against**: presence (who is doing what), concurrent editing with conflict handling, online / offline logic, realtime data via Company-OS. Rules now: ids and `updated_at` on every row, writes by id through the provider, lists driven by `subscribe`, no in-memory-only shared state; `version` column and presence table when the plan (`docs/reference/realtime-plan.md`) is approved. P-14. | prompt 0014, Justin | binding (Justin 2026-09-18) |
| D-208 | 2026-09-18 | **Build on the Company-OS framework at 2027+ technology strength**, not legacy patterns: agents as users, voice, realtime and multi-device are normal; no page reloads, one-user locks, hover-only UI or hard-coded values. Does not change D-183 (no wiring into Company-OS until Justin says so); the seams are where it connects. P-15. | prompt 0014, Justin ("super next gen systems ontop of our company os framework ... 2027+ technology") | binding (Justin 2026-09-18) |
| D-209 | 2026-09-18 | **`docs/platform-principles.md` is binding and sits above the module contract**; `CLAUDE.md` points to it and carries only the short rules (matrix widths, placeholder marking, actions manifest, en / es strings, surfaces.md). Principle ids `P-nn` are append-only like `D-nnn`. | prompt 0014 | decided |
| D-210 | 2026-09-18 | **Icon source = the Figma SVG exports.** The 29 SVGs in `docs/figma/exports/petrock-main/` (Iconly Two-tone rows, `Calendar` / `Message` / `Setting` / `User` / `Lock` / `Moon` / `Logout` / `Shield-Done` / `Info-Circle` / `Trash Bin` / `Arrow-Right`, `animal-rescue` / `hair-clipper` / `medal`) are converted once into the `ICON_SVGS` registry (`src/components/atom/Icon/figmaIcons.ts`) and rendered by the same `<Icon name>`; each keeps its own square viewBox (rescaling arc paths to 24 breaks flags), `currentColor` fills, two-tone opacity layers and fixed-colour layers only where the artwork has them. Bottom-nav and Services glyphs (`nav-*`, `svc-*`) are redrawn from the frames and stay **provisional** until the Figma Design System page export (~Sep 22) replaces them 1:1. Generic single-path outlines (`ICONS`) remain only where Figma shows no glyph. | prompt 0015 (Justin: "the icons ... fix this at the design system level"), prompt 0010 | proposed (implemented, needs Justin) |
| D-211 | 2026-09-18 | **Similarity index guides, never gates.** Every Figma pair is measured by `scripts/fidelity-composite.py` (block SSIM + global SSIM + colour-histogram overlap on the shared height, 0..1) with the side-by-side composite in `docs/design/fidelity/`; a fidelity pass reports before / after numbers per screen, but a pair is accepted by eye against the export, and a business rule (status vocabulary, location switcher, view switch) always wins over the score. | prompt 0013 / 0015 | decided |
| D-212 | 2026-09-18 | **Two modal scrims**: customer alert modals (`Modal size="alert"`, e.g. the Home Page gate) use `--color-scrim-soft` 0.20 as in `Home Page-5.png`; desk modals keep the 0.80 scrim seen in `front desk-3.jpg` (`--color-scrim`). Both dark-mode values are derived from the same alphas. | prompt 0013 (export measurement) | decided |
| D-213 | 2026-09-18 | **Timeline colours come from the export, as tokens**: alternating day columns `--color-grid-alt #CDD2D8` / `--color-grid-alt-head #B1B8C2`, hairlines and sidebar dividers `--color-border-grid #D9D9D9`, block fills `--timeline-<status>-fill` (confirmed `#B4F0A0`, checked_in `#7A7CE3`, checked_out `#FFF0B3`, pending_vaccines `#C9A6E9`, requested `#D18ACF`, daycare `#00D8D8`, plus cancelled / no_show) with dark-mode derivations in `tokens.ts`. The desk table badge hues stay on `bookingHues` (real lifecycle vocabulary) unless Justin asks for the `front desk-9.jpg` pairs. | prompt 0013 (`all reservation grooming.jpg`) | decided (badge hues: needs Justin) |
| D-214 | 2026-09-18 | **No photos cropped out of the Figma exports.** The hero pool photo, pet photos and room photos in the exports are 1x and unlicensed, so `HomeHero`, `PetAvatarCard` and `HotelRoomTypeCard` ship brand-tinted placeholders with the exact Figma footprint (142 px hero, 239 px room card) until licensed assets arrive (open question 116); placeholders must announce themselves per D-202. | prompt 0012 / 0013 | decided (assets: needs Justin) |
| D-215 | 2026-09-18 | **Customer bottom nav has the four Figma tabs** (Home, Bookings, Pets, Settings); the Chat tab is removed and Front Desk Chat is reached from Notification (split tab) and Settings. The unread dot stays on the paw tab. | prompt 0012 (`Home Page.png` Navbar) | proposed (implemented, needs Justin) |
| D-216 | 2026-09-18 | **Desk tables follow `front desk.jpg` structurally**: one framed white `DataTable` per page with ONE purple head, group rows inside the table (`groupBy`, e.g. "ARRIVING (34)"), every filter inside a `FilterPopover` tool button so the toolbar is one row, dot-less tinted `fill` badges; the page-code pill renders only in dev mode and the sidebar brand is the logo image. The D-014 expand / collapse-all and rail controls stay as hover-revealed icon buttons. | prompt 0010 part (c) (`front desk.jpg`, 598:23508..23564) | proposed (implemented, needs Justin) |
| D-217 | 2026-09-18 | **v0.2.0 is the design-fidelity release**; `docs/design/tokens-draft.md` is superseded as a source by `src/design/tokens.ts` (the only source) plus `docs/design/fidelity-audit.md` (the Figma-derived values with node ids) and stays as the historical extraction. Remaining fidelity deltas are tracked in `docs/design/fidelity/README.md` and the kanban, not in the draft. | prompt 0015 | decided |
| D-218 | 2026-09-20 | **Hub thumbnails are static, script-generated captures, never live iframes.** `scripts/hub-thumbs.mjs` (`npm run thumbs`) writes `docs/screenshots/<CODE>/thumb[-dark][-<label>].jpg` (1280x800 desktop viewport or 390x844 phone, dev mode off, as the card's demo person and location); the hub bundles them by `import.meta.glob` (`src/modules/hub/thumbs.ts`, `thumbFor` falls back label -> light -> wireframe). The old live PhoneFrame iframe shared the viewer's session and rendered "No access" for public viewers; files work on phones and 4K with no live-frame cap. Regenerate with `npm run thumbs` when a card's page changes; never hand-add. | prompt 0016 | decided |
| D-219 | 2026-09-20 | **One control per hub card.** The `Card` container is never interactive; the single `Button` ("Enter as <demo person>" / "Open") is the only focusable action; a Built / Stub / Planned pill comes from the route manifest and "You are here" shows when the viewer's effective role is the card's role and the card is that role's home; page codes only in dev mode. Applies to every future card grid (canvas, plan viewer). | prompt 0016 | decided |
| D-220 | 2026-09-20 | **Hub-scoped `--hub-scale` band** (1 / 1.375 at >= 2560 / 1.75 at >= 3840) drives the hub's type, spacing, medallions, thumbnails and content width until the global `--scale` token (D-194) lands; body >= 16 px at >= 1920. D-194 stays in the backlog. | prompt 0016 | decided |
| D-221 | 2026-09-20 | **`PageSpec.actions?: ActionDef[]` added** with the D-197 shape (`id '<module>.<verb>', label, intent, permission?, params?`); HUB-01 is the first page to declare actions (7). The actions bus, `/#/dev/actions` (D-20) and the `specCompleteness` check stay in the backlog so other pages' scores do not drop before the module-by-module fill. Partial implementation of D-197. | prompt 0016 | decided |
| D-222 | 2026-09-20 | **`Placeholder` atom lands** (D-201 / D-202) with the hub's three unbuilt tools as first consumers: Canvas (**D-21** `/dev/canvas`), Demo simulator (**D-22** `/dev/simulator`), Plan viewer (**D-23** `/plan`), codes reserved. Wiring `Placeholder` into `PageStub` and every toast-only handler is the remaining backlog card; its strings live in the hub table until a shared table exists. | prompt 0016 | decided |
| D-223 | 2026-09-20 | **Full-page captures settle images first**: `scripts/qa-lib.mjs` `settleImages(page)` scrolls the page and waits for every `img` to load before `screenshots.mjs` takes a full-page shot, because Chromium does not paint off-screen images at capture time. | prompt 0016 | decided |
| D-224 | 2026-09-20 | **The petrockhotel.com scrape is the content source set for the public website.** `docs/reference/petrockhotel-scrape/` (`INVENTORY.md`, one markdown file per page, the 50 original images, `all_images.json`, the hero-video reference) was crawled on 2026-09-20, the day the domain became reachable. It **supersedes Part A of `docs/reference/petrockhotel-site.md`** wherever the two differ (Part A stays as the 2026-09-18 search-index reconstruction). Re-crawl whenever the client changes the site and record the new date in `INVENTORY.md`; the site pages never quote a fact that is not in the source set. | prompt 0017, Justin ("please scrape the info and images and video from petrockhotel.com") | decided |
| D-225 | 2026-09-20 | **Site images pipeline.** `npm run site:images` (`scripts/site-images.mjs`, sharp) writes committed derivatives into `public/site/img/`: WebP at 480 / 960 / 1600 / 2560 (never upscaled past the source), a 1600 JPEG fallback, every file <= 450 KB, plus the logo as PNG. Pages render them only through the `SitePhoto` library component (`<picture>` + srcset, intrinsic width / height so nothing shifts, lazy unless `priority`); alt text is a translated string per slug (`extras-manual-website.photo.<slug>`). The derivatives are **committed** because CI runs only `npm run build` and sharp is a devDependency; never hand-edit a file in `public/site/img/`, re-run the script. | prompt 0017 | decided |
| D-226 | 2026-09-20 | **The website hero is the client's own media.** `SiteHero tone="media"` layers their YouTube background video (`youtube-nocookie`, id `htG1NJsfp24`) over its poster photo under a brand scrim (>= 4.5:1 for the white text, measured 7:1 average). The video mounts only above 700 px, only with `prefers-reduced-motion: no-preference`, and never under `navigator.webdriver`; the poster is always rendered, so phones, reduced-motion users, a blocked embed and every capture still show a real photo. A visible 44 px play / pause `IconButton` toggles it (nothing hover-only, D-195). Hero copy = eyebrow, `company.tagline`, the services subhead, the real 2011 lead, Book now and one `tel:` button per location from `locations`. | prompt 0017, Justin ("especially the hero section should integrate that stuff well") | decided |
| D-227 | 2026-09-20 | **P-13 `Website: Gallery`** at `/site/gallery`: the client's two Squarespace galleries rebuilt as one page (28 hotel photos + 12 spa photos from the scrape) with a `Modal` lightbox - previous / next arrows, Escape to close, an "n of N" counter and the photo's alt text as the caption. Every tile is a 44 px button, so touch, pen, keyboard and a future d-pad all work. It sits in `SITE_NAV` after Reviews. | prompt 0017 | decided |
| D-228 | 2026-09-20 | **Training & Fitness is informational on the site.** The section carries the client's real copy and a call-to-consult `tel:` CTA; **no training prices in the UI** until a `training_courses` table and a quote path exist, because a price on a page that does not come from a table violates R-X71. The published prices ($500 six-week course, $900 twelve-week combo, $95 seminars, $125 / $660 agility, $70 consultations) are recorded in `docs/reference/pricing-adoption-2026-09-20.md`. Owner question 14 (is training bookable in the app?) stays open. | prompt 0017 | decided |
| D-229 | 2026-09-20 | **Pricing seed adopted from petrockhotel.com per D-187** (full before / after in `docs/reference/pricing-adoption-2026-09-20.md`): rates Penthouse **135 / 150**, Suite **100 / 110**; two distinct bands - Seasonal (Easter - Labor Day) PH 145 / 160, Suite 110 / 120 and Holiday (Thanksgiving - New Year's) PH 165 / 175, Suite 120 / 130 - seeded with the 2026 dates, so adding a season row per year is an A-20 owner task; daycare cut-off **5 h** (data on `daycare_pricing.threshold_hours`, R-F01 resolved); the Gold / Platinum / Diamond grid by size; 5 add-ons repriced and 7 added (18 rows); prepaid daycare package discounts seeded (7 / 14 / 21 days at 5 / 7.5 / 10 %) but **not yet applied by `quoteDaycare`** (no purchase / redemption flow); `SEED_VERSION` 3 -> 4. **Not adopted**: the per-holiday "please call" prices, cat grooming, the large-dog accommodation fee and the dematting surcharge (no schema column or no published number); price ranges are seeded at their low end with the range in `notes`. Hotel long-stay discounts, the 2 % tax and the 3.89 % card fee are not published on the site and stay as they were. | prompt 0017, D-187 | decided (D-187); Justin to confirm the not-adopted list |
| D-230 | 2026-09-20 | **The legal entity is Petrock Holdings LLC** (from the site's privacy policy, "Privacy Policy for Petrock Hotel & Spa (Petrock Holdings LLC)"). It is `company.legalName` in `src/tenant/locations.ts`; the site footer and the About page read it from `company`, never from typed text. | prompt 0017 | decided |
| D-231 | 2026-09-21 | **One `ScheduleCard` for every appointment / booking card** on boards and grids: a 3 px lifecycle status stripe, the time, an assignee chip in the groomer's hue, pet + breed, customer, package / add-on / minutes chips, alert badges with tooltips that appear only when they apply, and exactly one 44 px "more" menu. The legacy R-G20 string stays as the card's accessible name and tooltip; the `compact` variant (day grid) shows at most one alert chip, or "N alerts" when several apply. **One `AppointmentBoard` organism** serves every Kanban board (F-14, F-31): status dot + label + count + collapse per column, compact empty columns, sideways scroll with a visible scrollbar and an edge fade. `GroomAppointmentCard` and `GroomStatusBoard` are retired and deleted. | prompt 0018, Justin ("Clean up the look and feel of the grooming views and hotel views") | decided |
| D-232 | 2026-09-21 | **The timeline grid is calm** (F-13): alternating day columns are a subtle tint (`color-grid-alt` #F6F7F9, head `color-grid-alt-head` #EEF1F4) instead of a grey block, weekends get `color-grid-weekend`, group rows are grey only in the label cell with a hairline across the row, the day header and the room column are sticky, today is a thin dashed line plus a small pill rather than a filled column, bars have a 6 px radius, a dark left edge (`color-bar-edge`) and a 13 px "Pet · Customer" label, a bar narrower than 76 px shows a "…" chip and keeps the full label in its `aria-label`, and the booking popover opens on click, Enter **or** hover (keyboard opening moves focus into it). Room cells show the room code plus a 2-letter type badge, and the group count pill stays in view. | prompt 0018 | decided |
| D-233 | 2026-09-21 | **Groomer identity uses the muted `groomerHues` palette** (`--groomer-1..6`, light + dark, in `src/design/tokens.ts`). Day-grid heads are white cards with an initials disc in the hue and a 3 px coloured top border; working hours are an 8 % tint of the hue, off hours a low-contrast hatch; overlapping appointments split the column into side-by-side lanes instead of stacking; the now line renders only when the shown day is today. The seeded groomer colours are remapped onto the palette (Encino's three groomers to hues 1-3, replacing the saturated #039B00 / #F4D06F / #7FDBDA), and `SEED_VERSION` goes 4 -> 5 so existing localStorage databases reseed. | prompt 0018 | decided |
| D-234 | 2026-09-21 | **Card actions live in one menu.** Status moves, Open and Assign are entries in the card's single "more" menu; PIN-gated moves carry a lock. Drag-and-drop stays as an optional accelerator and is never the only way to do something (D-195). No per-card "Move to" `Select` under a card. | prompt 0018 | decided |
| D-235 | 2026-09-21 | **Desk tables get a `num` tone** (`DataTable` `tone: 'num'`: tabular numerals in body colour) for times, dates, counts, phone numbers and money, so cells that are not links stop reading as links - a deliberate deviation from the Figma purple cells. Group rows become section rows: uppercase label + a count `Badge` + caret, a hairline above each section and a focusable 32 px button with a visible focus ring. The purple table head stays (D-216). | prompt 0018 | decided |
