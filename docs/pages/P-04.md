---
title: Website: Daycare
code: P-04
route: /site/daycare
roles: super_admin, owner, manager, front_desk, groomer, customer, public
status: built
module: extras-manual-website
figma: Day Care.png (placeholder screen)
---

# P-04 · Website: Daycare

## Purpose

Play - the day care area - in the client's own words: dogs interact, exercise, socialize and learn good social skills with their "pack leader" attendant, on an interactive jungle gym, on floors designed for their joints and bones. With a photo of the real yard, the price items and thresholds, the extra-pet discount, a typical day and capacity per location.

## Screenshots

| 390 | 1280 |
| --- | --- |
| ![390](../screenshots/P-04/390.jpg) | ![1280](../screenshots/P-04/1280.jpg) |

Dark: not captured for this page (key pages only).

## Sections (layout order)

1. SiteHero (soft)
2. Play area (SitePhoto + the client's copy + vaccine note)
3. Price tiles (full day, half day, play hour)
4. A typical day (steps)
5. Capacity per location
6. CtaBand

## Data

| Table | Read / write | Notes |
| --- | --- | --- |
| `daycare_pricing` | read | |
| `discounts` | read | |
| `capacities` | read | |
| `locations` | read | |

## Rules

- `R-F01` - see the rules registry (Settings › Rules) and the Rules tab of the page inspector
- `R-F02` - see the rules registry (Settings › Rules) and the Rules tab of the page inspector
- `R-E13` - see the rules registry (Settings › Rules) and the Rules tab of the page inspector
- `R-X71` - see the rules registry (Settings › Rules) and the Rules tab of the page inspector

## Logic

- The half / full-day cut-off is `daycare_pricing.threshold_hours`, now **5 h** from the live site (R-F01, D-229) - never a typed number.

## Photos used

`dsc06450` (the play yard), `dsc06434` (the pack at play).

## Actions (D-197)

| id | Label | Intent |
| --- | --- | --- |
| `site.daycare.book` | Book daycare | book a day of day care |

## Components

SiteLayout, SiteHero, **SitePhoto**, Button, Card, Icon

## Real vs mock

Copy and photos are the client's own, from the 2026-09-20 petrockhotel.com crawl (D-224), served as committed WebP derivatives from `public/site/img` through `SitePhoto` (D-225). Everything else reads the mock provider (localStorage) through the same `useTable` hooks the app uses; every price is computed from the pricing tables (R-X71), now seeded from the live site (D-229), never typed. Squarespace stays live at petrockhotel.com until this site replaces it. When Company-OS lands, `DataProvider` swaps and nothing on the page changes.

## Responsive check (D-016, D-194)

Checked at 360, 390, 768, 1280, 1920, 2560 and 3840 on 2026-09-20 (Playwright against `vite preview`): no horizontal page scroll at any width, body text >= 16 px at 1920+. Header nav becomes a slide-in drawer under 960 px; grids collapse to one column under 600 px; photos serve the 480 / 960 derivative on phones.

## Changelog

- `docs/changelog/0016-extras-manual-website.md` (first build)
- `docs/changelog/0031-petrockhotel-content-site-rebuild.md` (the client's copy and photos, SitePhoto, spec actions)
