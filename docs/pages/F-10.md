---
title: Hotel & Daycare reservations (table)
code: F-10
route: /desk/reservations
roles: super_admin, owner, manager, front_desk
status: built
module: frontdesk-reservations
figma: front desk.jpg, front desk-9.jpg, Frame 1171276264-10.png, front desk-10.jpg, front desk-11.jpg
---

# F-10 · Hotel & Daycare reservations (table)

## Purpose

The reservations table of the Figma v2 design (`front desk.jpg`): "+ Hotel Reservation" top-right, then one white container holding the "Hotel Reservations" title, the day navigator, a **Filters** popover (status, hotel vs daycare, room type, search, cancelled toggle) and **See All** (all upcoming) / **By day**; ONE purple 49 px head with head / row checkboxes, and the Arriving / Departing / Staying / Daycare / Checked out **group rows inside the same table** (R-I05), 18 columns with the Figma cell tones, 44 px rows, mint / tinted status badges. The Table / Timeline / Board switch implements D-008.

## Screenshots

| 390 | 1280 |
| --- | --- |
| ![390](../screenshots/F-10/390.jpg) | ![1280](../screenshots/F-10/1280.jpg) |

Dark: `../screenshots/F-10/390-dark.jpg`, `../screenshots/F-10/1280-dark.jpg`.

## Sections (layout order)

1. Top line - ViewSwitch (Table / Timeline / Board) left, "+ Hotel Reservation" right (R-I08)
2. Framed `DataTable` - title "Hotel Reservations", `ReservationDayNav`, `FilterPopover` (search, status, kind, room type, "Show cancelled / no show"), "See All" / "By day" toggle; one purple head; group rows "ARRIVING (n) ▾" ... "CHECKED OUT (n) ▾" (Checked out collapsed by default, empty groups say so); 18 columns (`lib/columns.tsx`, `RESERVATION_COLUMNS_FLAT`) with Open / Edit actions; "n rows · k selected" foot
3. Daycare drawer - summary of a daycare row (daycare check-in / pricing belong to F-40s)

## Data

| Table | Read / write | Notes |
| --- | --- | --- |
| `bookings`, `booking_pets`, `daycare_bookings` | read | joined by `useReservationRows()` |
| `customers`, `pets`, `rooms`, `room_types` | read | names, breeds, room codes |
| `vaccine_records`, `vaccine_types` | read | vaccine flag next to the status |

## Rules

- `R-I05` - Arriving / Departing / Staying / Checked out groups (plus Daycare)
- `R-I02`, `R-I01` - design chips Future / Checking In / Checked Out map to the lifecycle
- `R-D14`, `R-H08` - Balance = Total charge - Deposits
- `R-I08` - page-level "+ Hotel reservation" entry point
- `R-K01` - rows scoped to the current location

## Logic

- The 18 columns: ID, Status, Customer, Hotel room, Date in, Time in, Date out, Time out, Nbr days, Pet(s), Breed, Pet count, Mobile, Home, Total charge, Deposits, Balance, Booking notes.
- Nbr days = nights (hotel) or 1 (daycare); Home = customer alt phone.
- Cancelled / no-show rows hidden unless toggled.

## Components

SegmentedControl, ReservationDayNav, FilterPopover, Input, Select, Toggle, DataTable (framed, groupBy, selectable), Checkbox, StatusBadge, PetVaccineStatus, Drawer, BookingInfoGrid, Button, IconButton, EmptyState.

## Real vs mock

Mock provider. Open question 69 (what distinguishes DEPARTING from "Checking out") resolved by using the lifecycle buckets; see `docs/decisions/_pending/frontdesk-reservations.md`.

## Responsive check (D-016)

Checked at 360, 390, 768, 1280, 1920 (2026-09-18): no page overflow. Wide tables scroll inside the table card at 768-1280 and turn into cards under 768.

## Changelog

- `docs/changelog/_pending/frontdesk-reservations.md`
